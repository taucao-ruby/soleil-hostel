# ========== SECURITY: Token Authentication via httpOnly Cookie ==========
# Token lưu trong httpOnly cookie, KHÔNG localStorage → XSS không thể lấy được

# Domain cho cookie (MUST match frontend domain)
SESSION_DOMAIN=.soleilhostel.local

# Token cookie name (httpOnly + Secure + SameSite=Strict)
SANCTUM_COOKIE_NAME=soleil_token

# Token expiration (minutes)
SANCTUM_SHORT_LIVED_EXPIRATION_MINUTES=60

# Long-lived token expiration (days) - dùng cho remember me
SANCTUM_LONG_LIVED_EXPIRATION_DAYS=30

# Max refresh count per hour (detect suspicious activity)
SANCTUM_MAX_REFRESH_COUNT_PER_HOUR=10

# Token rotation: rotate sau mỗi refresh (mitigates token theft)
SANCTUM_ROTATE_TOKENS=true

# Device fingerprint verification (optional, higher security)
SANCTUM_VERIFY_DEVICE_FINGERPRINT=false
