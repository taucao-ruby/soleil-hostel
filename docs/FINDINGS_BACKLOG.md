# Findings Backlog — Soleil Hostel

Code issues found during audits (2026-02-21, 2026-03-20). **DO NOT FIX** — document only.

Severity guide:

- **Critical**: data integrity, security, booking invariant violation
- **High**: incorrect behavior visible to users
- **Medium**: tech debt, inconsistency, performance
- **Low**: style, minor inaccuracy

| ID   | File:Line                                                                                             | Issue                                                                                                                                                                                                                                                                                                                                                                                                                          | Severity | Suggested Fix                                                                                                                                                                            | Status                                              |
| ---- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| F-01 | `docs/DATABASE.md:148`                                                                                | Claims `CREATE TYPE room_status AS ENUM ('available', 'occupied', 'maintenance')` but no such CREATE TYPE exists. Rooms use `string('status')` (VARCHAR) in migration `2025_05_09_000000`.                                                                                                                                                                                                                                     | Medium   | Update DATABASE.md to reflect that room_status is a VARCHAR column, not a PG ENUM. Document actual valid values from application code.                                                   | **Fixed** (PR-4)                                    |
| F-02 | `docs/README.md:162`                                                                                  | Tech Stack table says "Laravel 11 + PHP 8.3" but `composer.json` requires `laravel/framework:^12.0` and `php:^8.2`.                                                                                                                                                                                                                                                                                                            | Low      | Update to "Laravel 12 + PHP 8.2+".                                                                                                                                                       | **Fixed** (confirmed audit v4, 2026-02-23)          |
| F-03 | `docs/README.md:155`                                                                                  | Project Status table says "142 frontend unit tests" but COMPACT.md reports 145 tests (13 suites) as of 2026-02-21.                                                                                                                                                                                                                                                                                                             | Low      | Update test count to 145.                                                                                                                                                                | **Fixed** (batch-4, all 142→145 references updated) |
| F-04 | `.github/workflows/tests.yml:8-9`                                                                     | CI triggers on `push: branches: [main, develop]` but repo uses `dev` branch, not `develop`. Pushes to `dev` will NOT trigger CI.                                                                                                                                                                                                                                                                                               | High     | Change `develop` to `dev` in both `on.pull_request.branches` and `on.push.branches`.                                                                                                     | **Fixed** (PR-1)                                    |
| F-05 | `.github/workflows/tests.yml` (multiple)                                                              | CI uses `pnpm` for frontend (install, test, lint, build) but local dev docs (AGENTS.md, COMPACT.md, docs/README.md Quick Start) reference `npm`.                                                                                                                                                                                                                                                                               | Medium   | Align docs to mention both, or standardize. Frontend `package.json` has both `npm` and no pnpm lockfile locally, but CI uses pnpm and a `pnpm-lock.yaml` exists.                         | **Fixed** (PR-4)                                    |
| F-06 | `backend/database/migrations/*`                                                                       | No `CHECK (check_out > check_in)` constraint on bookings table. DB_FACTS.md correctly notes this absence.                                                                                                                                                                                                                                                                                                                      | Medium   | Add migration with `ALTER TABLE bookings ADD CONSTRAINT chk_bookings_dates CHECK (check_out > check_in)`.                                                                                | **Fixed** (PR-2)                                    |
| F-07 | `backend/database/migrations/*`                                                                       | No `CHECK (rating BETWEEN 1 AND 5)` constraint on reviews table. DB_FACTS.md correctly notes this absence.                                                                                                                                                                                                                                                                                                                     | Medium   | Add migration with `ALTER TABLE reviews ADD CONSTRAINT chk_reviews_rating CHECK (rating BETWEEN 1 AND 5)`.                                                                               | **Fixed** (PR-2)                                    |
| F-08 | `backend/database/migrations/*`                                                                       | No `CHECK (price >= 0)` constraint on rooms table.                                                                                                                                                                                                                                                                                                                                                                             | Low      | Add migration with `ALTER TABLE rooms ADD CONSTRAINT chk_rooms_price CHECK (price >= 0)`.                                                                                                | **Fixed** (PR-2)                                    |
| F-09 | `docs/DB_FACTS.md:29`                                                                                 | States "FK `reviews.booking_id -> bookings.id`: Not found in migrations"                                                                                                                                                                                                                                                                                                                                                       | Medium   | Add foreign key constraint migration for `reviews.booking_id`.                                                                                                                           | **Fixed** (PR-3)                                    |
| F-10 | `docs/KNOWN_LIMITATIONS.md:120`                                                                       | Contains `// TODO: Integrate with Stripe`                                                                                                                                                                                                                                                                                                                                                                                      | Low      | Track Stripe integration as a planned feature, not a TODO in docs.                                                                                                                       | **Fixed** (PR-4)                                    |
| F-11 | `docs/frontend/PERFORMANCE_SECURITY.md:62`                                                            | Contains `analytics integration (TODO)`                                                                                                                                                                                                                                                                                                                                                                                        | Low      | Track analytics as planned feature.                                                                                                                                                      | **Fixed** (PR-4)                                    |
| F-12 | `docs/frontend/UTILS_LAYER.md:99`                                                                     | Contains `Analytics service integration (TODO - currently commented out)`                                                                                                                                                                                                                                                                                                                                                      | Low      | Same as F-11.                                                                                                                                                                            | **Fixed** (PR-4)                                    |
| F-13 | Booking status type                                                                                   | Booking `status` is VARCHAR, not a PG ENUM (unlike `user_role_enum`). Values `pending`, `confirmed`, `refund_pending`, `cancelled`, `refund_failed` are enforced only at app level.                                                                                                                                                                                                                                            | Low      | Consider creating `CREATE TYPE booking_status AS ENUM (...)` for DB-level enforcement, or document this as intentional.                                                                  | **Fixed** (PR-4, documented as intentional)         |
| F-14 | `docker-compose.yml:40`                                                                               | Redis password default `soleil_redis_secret_2026` is hardcoded in docker-compose.yml (visible in repo).                                                                                                                                                                                                                                                                                                                        | Medium   | Use env var without default, or document that this is local-dev-only and acceptable.                                                                                                     | **Fixed** (PR-1)                                    |
| F-15 | `backend/.env.test`                                                                                   | Tracked in git with `DB_CONNECTION=mysql` — contradicts PostgreSQL requirement. Contains placeholder credentials.                                                                                                                                                                                                                                                                                                              | Medium   | Remove from git or update to PostgreSQL config.                                                                                                                                          | **Fixed** (batch-2)                                 |
| F-16 | `.github/workflows/tests.yml:397,421,467,490`                                                         | Pint, Psalm, Composer Audit, NPM Audit all have `continue-on-error: true` — quality gates are non-blocking.                                                                                                                                                                                                                                                                                                                    | Medium   | Set `continue-on-error: false` for at minimum Pint and Composer Audit.                                                                                                                   | **Fixed** (batch-1)                                 |
| F-17 | `backend/.env.testing:3`                                                                              | Committed `APP_KEY=base64:...` — test-only but committed encryption keys are a hygiene issue.                                                                                                                                                                                                                                                                                                                                  | Low      | Set `APP_KEY=` (empty) and let CI generate it.                                                                                                                                           | **Fixed** (batch-2)                                 |
| F-18 | `frontend/src` (multiple files)                                                                       | 8 TODO markers including `console.log` in SearchCard.tsx production code.                                                                                                                                                                                                                                                                                                                                                      | Low      | Remove console.log; track TODOs in backlog.                                                                                                                                              | **Fixed** (batch-3)                                 |
| F-19 | `docs/DEVELOPMENT_HOOKS.md:23`                                                                        | Says "142 tests" for frontend; actual: 145 tests. Residual of F-03.                                                                                                                                                                                                                                                                                                                                                            | Low      | Update test count.                                                                                                                                                                       | **Fixed** (batch-4)                                 |
| F-20 | CI workflows                                                                                          | No `docker compose config` validation in CI despite being listed as DoD gate in agents/CONTRACT.md.                                                                                                                                                                                                                                                                                                                            | Low      | Add lightweight CI job running `docker compose config`.                                                                                                                                  | **Fixed** (batch-1)                                 |
| F-21 | `frontend/src/features/auth/LoginPage.tsx` + `RegisterPage.tsx`                                       | UI labels in English ("Welcome Back", "Create Account", etc.) instead of Vietnamese. App convention is Vietnamese for all user-facing copy.                                                                                                                                                                                                                                                                                    | Medium   | Translate all user-facing strings to Vietnamese, consistent with BookingForm/Dashboard.                                                                                                  | **Fixed**                                           |
| F-22 | `backend/app/Http/Controllers/Auth/HttpOnlyTokenController.php:290`                                   | String literal `'Token cookie không ditemukan.'` is Indonesian, not Vietnamese. All other user-facing strings in this file are Vietnamese. Discovered during TD-002 (out of scope — string literal, not a comment).                                                                                                                                                                                                            | Low      | Replace with Vietnamese equivalent: `'Token cookie không tìm thấy.'`                                                                                                                     | **Fixed**                                           |
| F-23 | `docs/COMPACT.md` (sections 1–7)                                                                      | Pre-existing Markdown lint warnings: MD022 (headings not surrounded by blank lines), MD032 (lists not surrounded by blank lines), MD031 (fenced code blocks not surrounded by blank lines) across the static sections of the file. Introduced before Phase 5 session.                                                                                                                                                          | Low      | Add blank lines between headings and their immediately-following list/code-block content. No logic change — formatting only.                                                             | Open                                                |
| F-24 | `backend/app/Http/Controllers/Auth/AuthController.php:115,233` + `HttpOnlyTokenController.php:92,199` | Auth controllers use `DB::table('personal_access_tokens')->insertGetId()` (Query Builder) to create tokens, bypassing Eloquent model events. Cannot simply switch to `PersonalAccessToken::create()` because the `HasUuids` trait generates UUID primary keys while the DB column is `bigIncrements` (integer). Requires either: (a) override `uniqueIds()` on `PersonalAccessToken` to exclude PK, or (b) migrate PK to UUID. | High     | Override `uniqueIds()` on `PersonalAccessToken` to return `['token_identifier']` instead of the primary key, then replace `DB::table()` calls with Eloquent `$user->tokens()->create()`. | **Fixed** (H-02, 2026-03-06)                        |
| F-25 | `frontend/src/shared/lib/api.ts` — 401 refresh interceptor | `refreshResponse.data.csrf_token` reads wrong level; actual path is `refreshResponse.data.data.csrf_token` (wrapped response). Non-critical: `check_httponly_token` routes do not validate CSRF so the misread value is never enforced. Identified 2026-03-14. | Low | Fix to `refreshResponse.data.data.csrf_token` in the 401 refresh-interceptor success branch. | Open |

## 2026-03-20 Audit Findings — Backend Security

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-26 | `backend/app/Services/BookingService.php:84-121` | `confirmBooking()` runs inside a `DB::transaction` without `lockForUpdate()`. Two concurrent Stripe webhooks or admin confirms can both pass the status pre-check and double-write. `CancellationService` uses the lock correctly; this method does not. | Critical | Add `Booking::query()->where('id', $booking->id)->lockForUpdate()->first()` at top of the transaction closure, re-check status on the locked row before updating. | Open |
| F-27 | `backend/app/Http/Controllers/AdminBookingController.php:106-144` | `restore()` checks for overlapping bookings outside a transaction, then calls `bookingService->restore()` in a separate operation — a TOCTOU race. A concurrent booking creation can fill the slot between the overlap check and the restore. | Critical | Wrap both `hasOverlappingBookings` and `bookingService->restore()` in a single `DB::transaction` with `lockForUpdate()` on the target booking row. | Open |
| F-28 | `backend/app/Http/Controllers/AdminBookingController.php:194-241` | `restoreBulk()` has the same TOCTOU race as `restore()`: per-booking overlap check and restore are two separate DB operations without transaction + lock. | Critical | Wrap each iteration's check-and-restore in `DB::transaction` + `lockForUpdate()`. | Open |
| F-29 | `backend/app/Http/Controllers/AuthController.php:53,87` | Legacy `AuthController::register()` and `login()` call `$user->createToken()` without setting `expires_at`. Resulting tokens bypass the custom `CheckTokenNotRevokedAndNotExpired` middleware entirely — effectively immortal until manually deleted. Endpoints are deprecated but still live. | High | Set `expires_at` on the token at creation time in the legacy controller (e.g. `now()->addDays(30)`), or add a hard date gate expiring all legacy tokens on the July 2026 sunset date. | Open |
| F-30 | `backend/routes/api.php:88-91` | `GET /api/auth/csrf-token` has no auth middleware and no rate limiting. Returns a live CSRF token to any unauthenticated caller. | High | Add `throttle:10,1` at minimum; if only needed post-login, add `check_token_valid` middleware. | Open |
| F-31 | `backend/app/Http/Controllers/Auth/UnifiedAuthController.php:154-196` | `detectAuthMode()` uses `config('sanctum.max_refresh_count', 50)` as the suspicious-activity threshold. Every other location in the codebase uses `config('sanctum.max_refresh_count_per_hour', 10)`. The wrong key allows 50 refreshes before flagging theft — 5× the intended limit. | High | Change key to `sanctum.max_refresh_count_per_hour` with default `10`. | Open |
| F-32 | `backend/app/Http/Controllers/Auth/UnifiedAuthController.php:154-196` | `detectAuthMode()` Bearer lookup queries `PersonalAccessToken::where('token', $tokenHash)` by hashing the full `{id}\|{token}` string. Sanctum stores only the hash of the random portion. The lookup will always return `null` — the Bearer path in `detectAuthMode()` is silently broken, returning 401 for all valid Bearer tokens on `/auth/unified/*`. | High | Replace with `PersonalAccessToken::findToken($bearerToken)` as used in `CheckTokenNotRevokedAndNotExpired`. | Open |
| F-33 | `backend/app/Services/CancellationService.php:268-282` | `finalizeCancellation()` runs inside a `DB::transaction` without `lockForUpdate()` on the booking. Between `processRefund()` succeeding and the status update to `CANCELLED`, a concurrent request can observe `refund_pending` and re-enter the refund flow. | Medium | Add `Booking::query()->where('id', $booking->id)->lockForUpdate()->first()` at the top of the `finalizeCancellation` closure. | Open |
| F-34 | `backend/app/Http/Controllers/ContactController.php:43` | `$request->input('per_page', 15)` is passed directly to `getPaginated()` without validation or clamping. `per_page=100000` causes a massive un-paginated DB scan. | Medium | Clamp: `$perPage = min((int) $request->input('per_page', 15), 100)` or use a FormRequest with `integer\|min:1\|max:100`. | Open |
| F-35 | `backend/app/Http/Controllers/Auth/HttpOnlyTokenController.php:40-141` | `login()` accesses `$request->input('email')` and `$request->input('password')` directly without a FormRequest. Missing presence/type/length validation makes the endpoint susceptible to type-coercion edge cases (e.g. array inputs). `LoginRequest` exists but is not used here. | Medium | Inject `LoginRequest` (already exists in `app/Http/Requests/Auth/`) instead of raw `$request->input()`. | Open |
| F-36 | `backend/app/Http/Controllers/Auth/HttpOnlyTokenController.php:209` + `backend/app/Http/Middleware/CheckHttpOnlyTokenValid.php:82` | Fence-post inconsistency: `refresh()` uses `>=` for the suspicious-activity threshold check; `CheckHttpOnlyTokenValid` uses `>`. A token at the exact threshold value passes middleware but is rejected on next refresh — one extra use of a token that should already be revoked. | Medium | Align both to `>=` for strict enforcement at the limit. | Open |
| F-37 | `backend/app/Services/CustomerService.php:46` | `getCustomerProfile(string $email)` uses `"customer_profile_{$email}"` as a raw cache key. Emails with `:`, `{`, `}`, `(`, `)`, `/`, `\`, `@` characters can collide with or pollute other cache entries depending on driver. | Medium | Hash the email: `"customer_profile_".md5($email)`. | Open |
| F-38 | `backend/app/Http/Controllers/Payment/StripeWebhookController.php:144-159` | `handleChargeRefunded()` runs inside a `DB::transaction` without `lockForUpdate()` on the booking row. Low probability given Stripe delivery guarantees, but inconsistent with the project's locking discipline. | Low | Add `$booking->lockForUpdate()` or re-query with lock inside the transaction closure. | Open |
| F-39 | `backend/app/Http/Middleware/CheckHttpOnlyTokenValid.php:36-46` | Manual fallback cookie parsing via `strpos`/`substr` does not `urldecode()` the extracted value. URL-encoded token identifiers (unlikely but possible) will fail hash comparison silently. | Low | Apply `urldecode()` to the extracted cookie value, or rely exclusively on `$request->cookie()` which Laravel decodes automatically. | Open |
| F-40 | `frontend/src/shared/lib/api.ts:158-161` | On refresh failure, code calls `localStorage.removeItem` for `auth_token` and `refresh_token`. Implies these values may be stored in `localStorage` (XSS-reachable). If any code path writes Bearer tokens to `localStorage`, this is a HIGH security issue. If no code does, the calls are dead code masking a latent risk. | Low | Audit all `localStorage.setItem` calls for auth keys. Remove the `localStorage.removeItem` calls if no code writes tokens there. | Open |
| F-41 | `backend/app/Http/Middleware/CheckTokenNotRevokedAndNotExpired.php:36-44` | When no Bearer token is present and no guard has a user, the middleware directly instantiates `CheckHttpOnlyTokenValid::handle()` — bypassing Laravel's IoC container and middleware resolution pipeline. Fragile against future constructor injection on `CheckHttpOnlyTokenValid`. | Medium | Use `app()->make(CheckHttpOnlyTokenValid::class)->handle(...)` for proper IoC resolution, or restructure the middleware stack. | Open |

## 2026-03-20 Audit Findings — Database Integrity

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-42 | `backend/app/Models/Room.php:183-188` | `scopeAvailableBetween` uses `whereDoesntHave('bookings', ...)` without an explicit `whereNull('bookings.deleted_at')`. Soft-delete exclusion relies on the implicit Eloquent global scope; the dual-layer invariant requires it to be explicit. | Medium | Add `->whereNull('bookings.deleted_at')` inside the `whereDoesntHave` closure as defense-in-depth. | Open |
| F-43 | `backend/app/Repositories/EloquentRoomRepository.php:82` | `hasOverlappingConfirmedBookings()` queries `status = 'confirmed'` only, missing `pending`. This is inconsistent with `Booking::ACTIVE_STATUSES` (`[pending, confirmed]`) and the DB EXCLUDE constraint — `pending` bookings are invisible to this overlap check. | Medium | Change to `->whereIn('status', ['pending', 'confirmed'])` or use `BookingStatus::activeStatuses()`. | Open |
| F-44 | `backend/database/migrations/2025_11_24_000000_create_reviews_table.php:41` + `backend/app/Models/Review.php:39-41` | `reviews.approved` column has DB-level default `true` (migration), but Eloquent `$attributes` sets default `false` (model). Any raw `INSERT` bypassing the model (seeders, `DB::table`, queue jobs) silently auto-approves reviews. | Medium | Add a migration: `ALTER TABLE reviews ALTER COLUMN approved SET DEFAULT false`. | Open |
| F-45 | `backend/app/Http/Controllers/AdminBookingController.php:203-235` + `backend/app/Services/BookingService.php:391-396` | `restoreBulk()` calls `getTrashedBookingById()` in a `foreach` loop — one SELECT per booking ID (N+1). | Medium | Load all trashed bookings in a single query before the loop: `Booking::onlyTrashed()->with(['room','user'])->whereIn('id', $ids)->get()->keyBy('id')`. | Open |
| F-46 | `backend/database/migrations/2026_02_22_000002_add_fk_reviews_booking_id.php:26,41` | Uses `config('database.default') === 'sqlite'` driver guard (weaker pattern). All post-2026-03-17 migrations correctly use `DB::getDriverName() !== 'pgsql'`. Can produce false-negatives if `database.default` is overridden at runtime. | Medium | Change to `DB::getDriverName() !== 'pgsql'` to match project-wide convention. (Same applies to `2026_02_09_000000` — cleanup deferred per ARCHITECTURE_FACTS note.) | Open |
| F-47 | `backend/database/migrations/2026_02_11_000000_reconcile_legacy_index_ordering.php:52-55` | `down()` is an empty no-op. Rolling back this migration does not remove indexes added in `up()`. Subsequent `down()` migrations attempting to `dropIndex` by name on those indexes could fail depending on order. | Medium | Implement `down()` to drop each index added in `up()`, wrapped in `hasIndex` existence checks for idempotency. | Open |
| F-48 | `backend/app/Models/Location.php` | `lock_version` is absent from `Location::$casts`. Reads return `lock_version` as a string. The optimistic-locking CAS query uses integer comparison — type mismatch could cause silent failures if the driver returns strings. (`Room.php` correctly casts `lock_version` to `integer`.) | Low | Add `'lock_version' => 'integer'` to `Location::$casts`. | Resolved (Wave 3 PR-3D) |
| F-49 | `backend/app/Services/BookingService.php:111` + `backend/app/Http/Controllers/BookingController.php:225` | `confirmBooking()` calls `$booking->user->notify(...)` inside a transaction without eager-loading `user`. Triggers a lazy-load query inside the lock-hold window, unnecessarily extending transaction duration. | Low | Eager-load `user` before calling `confirmBooking()`, or dispatch notification via `afterCommit` using `TransactionCommitting`. | Open |

## 2026-03-20 Audit Findings — Frontend

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-50 | `frontend/src/shared/utils/toast.ts:1-2` + `frontend/package.json:25` | `react-toastify` is a forbidden third-party toast library (rule: no `react-hot-toast` or equivalents). `toast.ts` wraps it; `ToastContainer` is imported in `main.tsx`. The `showToast` abstraction means call-sites need no change if the library is replaced. | Critical | Replace `react-toastify` with an in-house toast implementation (React context + portal). The `showToast` API surface is already abstracted. | Open |
| F-51 | `frontend/src/features/booking/BookingList.tsx:16-33` + `frontend/src/features/auth/AuthContext.tsx:175` + `frontend/src/features/booking/BookingForm.tsx:128` | `BookingList.tsx` uses a manual `active` flag instead of the required `AbortController` pattern and never passes a signal to `fetchMyBookings`. `AuthContext.tsx` and `BookingForm.tsx` use `as { response?: ... }` type assertions on caught errors instead of `isAxiosError()` narrowing. | Critical | Replace `active` flag with AbortController + pass `signal` to `fetchMyBookings`. Use `isAxiosError(err)` (already imported in `AuthContext.tsx`) for safe narrowing. | Open |
| F-52 | `frontend/src/features/admin/rooms/RoomForm.tsx:36-69`, `frontend/src/features/admin/bookings/TodayOperations.tsx:18-23,40-43`, `frontend/src/features/admin/bookings/BookingCalendar.tsx:31-52`, `frontend/src/features/admin/bookings/AdminBookingDashboard.tsx:27-34,55-60`, `frontend/src/features/admin/customers/CustomerProfile.tsx:17-39`, `frontend/src/features/admin/customers/CustomerList.tsx:18-24` | 7 `useEffect` hooks across 5 admin components make API calls via `.then()` chains with no `AbortController` and no cleanup return. State updates fire on unmounted components; some also have `eslint-disable` comments suppressing missing-deps warnings. | High | Add `const controller = new AbortController()` to each effect, pass `{ signal: controller.signal }` to each `api.get()` call, and return `() => controller.abort()`. Remove `eslint-disable` comments. | Open |
| F-53 | `frontend/src/features/admin/bookings/adminBooking.api.ts:70-84` | `getTodayArrivals()` and `getTodayDepartures()` return `response.data.data` typed as `BookingDetailRaw[]`, but the admin bookings endpoint wraps in `{ bookings, meta }`. The correct expression is `response.data.data.bookings`. This type mismatch silently returns `undefined` at runtime. | Medium | Fix return expression to `response.data.data.bookings` and align the return type. | Open |
| F-54 | `frontend/src/features/admin/bookings/BookingCalendar.tsx:46-51`, `frontend/src/features/admin/rooms/RoomForm.tsx:38`, `frontend/src/features/admin/rooms/AdminRoomDashboard.tsx:26-29`, `frontend/src/features/admin/bookings/TodayOperations.tsx:19`, `frontend/src/features/admin/bookings/AdminBookingDashboard.tsx:28-33` | 5 components call `api.get('/v1/locations')` directly instead of going through a dedicated API module function. Rule: all API calls must go through named functions in `*.api.ts` files; `getLocations()` already exists in `@/shared/lib/location.api`. | Medium | Replace all direct `api.get('/v1/locations')` calls with `getLocations()` from `@/shared/lib/location.api`. For rooms, use `getRoomsByLocation()` from `adminRoom.api.ts`. | Open |
| F-55 | `frontend/src/features/admin/bookings/BookingCalendar.tsx:175` | `window.location.href = \`/admin/bookings/${booking.id}\`` causes a full page reload. Should use React Router `useNavigate`. | Medium | Replace with `navigate(\`/admin/bookings/${booking.id}\`)` using `useNavigate()`. | Open |
| F-56 | `frontend/src/features/admin/bookings/AdminBookingTable.tsx:31` | `window.prompt()` used for collecting cancellation reason — a blocking browser-native dialog, inconsistent with the rest of the UI which uses `ConfirmDialog`. | Medium | Replace with a custom modal/dialog component using React state, consistent with `ConfirmDialog` used elsewhere. | Open |
| F-57 | `frontend/src/features/home/components/SearchCard.test.tsx:9-13`, `frontend/src/features/booking/BookingForm.test.tsx:55-58`, `frontend/src/features/bookings/GuestDashboard.test.tsx:11-12` | `mockNavigate` and `mockCreateBooking` are declared as module-level `vi.fn()` variables captured by `vi.mock()` factory closures — the exact hoisting failure pattern in Vitest 2.x jsdom. Will cause flaky CI failures on a Vitest upgrade. | Medium | Move affected mocks into `vi.hoisted()`: `const { mockNavigate } = vi.hoisted(() => ({ mockNavigate: vi.fn() }))`. | Open |
| F-58 | `frontend/src/app/router.tsx:88`, `frontend/src/features/auth/ProtectedRoute.tsx:34`, `frontend/src/features/admin/rooms/RoomForm.tsx:109` | Three English UI strings in user-facing code: `"Loading..."` (Suspense fallback), `"Checking authentication..."` (ProtectedRoute), `"Version conflict. Please refresh."` (RoomForm error). All user-facing text must be Vietnamese. | Medium | Change to `"Đang tải..."`, `"Đang xác thực..."`, `"Xung đột phiên bản. Vui lòng tải lại trang."` respectively. | Open |
| F-59 | `frontend/src/features/booking/BookingList.tsx` | No error state is tracked. If `fetchMyBookings` fails, the component silently stays in empty state with `isLoading: false`. Inconsistent with `GuestDashboard` and `RoomList` which surface errors to the user. | Medium | Add `const [error, setError] = useState<string \| null>(null)` and render it in the UI. | Open |
| F-60 | `frontend/src/features/admin/bookings/adminBooking.api.ts:4-12` | `AdminBookingsResponse` is declared in this file (shape: `{ bookings, meta }`) and also in `admin.types.ts` (shape: `{ data: { bookings, meta } }`). Two conflicting types with the same name in the same feature. | Low | Remove the duplicate from `adminBooking.api.ts` and import from `admin.types.ts`, or rename one clearly. | Open |
| F-61 | `frontend/src/features/admin/bookings/TodayOperations.tsx:49-50` | `lock_version: 1` is hardcoded with comment `/* ignoring lock check for brevity */`. This bypasses the optimistic locking contract. | Low | Remove the hardcoded value and properly read `lock_version` from the room/booking entity, or formally defer with a tracked backlog item. | Open |
| F-62 | `frontend/src/features/locations/LocationDetail.tsx:312` | Room price displayed as `$${room.price}/night` (dollar sign, English "night"). All other price displays use `formatVND` and Vietnamese strings. | Medium | Replace with `{formatVND(room.price)}<span>/đêm</span>`. | Open |
