# Findings Backlog — Soleil Hostel

Code issues found during audits (2026-02-21, 2026-03-20, 2026-04-05, 2026-04-18). **DO NOT FIX** — document only.

Severity guide:

- **Critical**: data integrity, security, booking invariant violation
- **High**: incorrect behavior visible to users
- **Medium**: tech debt, inconsistency, performance
- **Low**: style, minor inaccuracy

**F-ID namespace note (2026-04-19)**: the AI-harness proposer-binding finding from the 2026-04-18 remediation batch was informally cited as "F-06 (2026-04-18)" in ARCHITECTURE_FACTS.md, PERMISSION_MATRIX.md, THREAT_MODEL_AI.md and early WORKLOG entries. That ID collides with the 2026-02-21 F-06 (CHECK `check_out > check_in` constraint) already in this backlog. The proposer-binding finding has been promoted to **F-67** (see the 2026-04-18 section below). Historical commit messages and append-only WORKLOG entries that still cite "F-06 (2026-04-18)" are preserved as-is; live docs have been swept. `AUDIT_2026_03_12_STRUCTURE.md` maintains its own F-IDs scoped to that document — do not cross-reference.

| ID   | File:Line                                                                                             | Issue                                                                                                                                                                                                                                                                                                                                                                                                                          | Severity | Suggested Fix                                                                                                                                                                            | Status                                              |
| ---- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| F-01 | `docs/DATABASE.md:148`                                                                                | Claims `CREATE TYPE room_status AS ENUM ('available', 'occupied', 'maintenance')` but no such CREATE TYPE exists. Rooms use `string('status')` (VARCHAR) in migration `2025_05_09_000000`.                                                                                                                                                                                                                                     | Medium   | Update DATABASE.md to reflect that room_status is a VARCHAR column, not a PG ENUM. Document actual valid values from application code.                                                   | **Fixed** (PR-4)                                    |
| F-02 | `docs/README.md:162`                                                                                  | Tech Stack table says "Laravel 11 + PHP 8.3" but `composer.json` requires `laravel/framework:^12.0` and `php:^8.2`.                                                                                                                                                                                                                                                                                                            | Low      | Update to "Laravel 12 + PHP 8.2+".                                                                                                                                                       | **Fixed** (confirmed audit v4, 2026-02-23)          |
| F-03 | `docs/README.md:155`                                                                                  | Project Status table says "142 frontend unit tests" but COMPACT.md reports 145 tests (13 suites) as of 2026-02-21.                                                                                                                                                                                                                                                                                                             | Low      | Update test count to 145.                                                                                                                                                                | **Fixed** (batch-4, all 142→145 references updated) |
| F-04 | `.github/workflows/tests.yml:8-9`                                                                     | CI triggers on `push: branches: [main, develop]` but repo uses `dev` branch, not `develop`. Pushes to `dev` will NOT trigger CI.                                                                                                                                                                                                                                                                                               | High     | Change `develop` to `dev` in both `on.pull_request.branches` and `on.push.branches`.                                                                                                     | **Fixed** (PR-1)                                    |
| F-05 | `.github/workflows/tests.yml` (multiple)                                                              | CI uses `pnpm` for frontend (install, test, lint, build) but local dev docs (AGENTS.md, COMPACT.md, docs/README.md Quick Start) reference `npm`.                                                                                                                                                                                                                                                                               | Medium   | Align docs to mention both, or standardize. Frontend `package.json` has both `npm` and no pnpm lockfile locally, but CI uses pnpm and a `pnpm-lock.yaml` exists.                         | **Fixed** (PR-4)                                    |
| F-06 | `backend/database/migrations/*`                                                                       | No `CHECK (check_out > check_in)` constraint on bookings table. DB_FACTS.md correctly notes this absence.                                                                                                                                                                                                                                                                                                                      | Medium   | Add migration with `ALTER TABLE bookings ADD CONSTRAINT chk_bookings_dates CHECK (check_out > check_in)`.                                                                                | **Fixed** (PR-2)                                    |
| F-07 | `backend/database/migrations/*`                                                                       | No `CHECK (rating BETWEEN 1 AND 5)` constraint on reviews table. DB_FACTS.md correctly notes this absence.                                                                                                                                                                                                                                                                                                                     | Medium   | Add migration with `ALTER TABLE reviews ADD CONSTRAINT chk_reviews_rating CHECK (rating BETWEEN 1 AND 5)`.                                                                               | **Fixed** (PR-2)                                    |
| F-08 | `backend/database/migrations/*`                                                                       | No `CHECK (price >= 0)` constraint on rooms table.                                                                                                                                                                                                                                                                                                                                                                             | Low      | Add migration with `ALTER TABLE rooms ADD CONSTRAINT chk_rooms_price CHECK (price >= 0)`.                                                                                                | **Fixed** (PR-2)                                    |
| F-09 | `docs/DB_FACTS.md:29`                                                                                 | States "FK `reviews.booking_id -> bookings.id`: Not found in migrations"                                                                                                                                                                                                                                                                                                                                                       | Medium   | Add foreign key constraint migration for `reviews.booking_id`.                                                                                                                           | **Fixed** (PR-3)                                    |
| F-10 | `docs/KNOWN_LIMITATIONS.md:120`                                                                       | Contains `// TODO: Integrate with Stripe`                                                                                                                                                                                                                                                                                                                                                                                      | Low      | Track Stripe integration as a planned feature, not a TODO in docs.                                                                                                                       | **Fixed** (PR-4)                                    |
| F-11 | `docs/frontend/PERFORMANCE_SECURITY.md:62`                                                            | Contains `analytics integration (TODO)`                                                                                                                                                                                                                                                                                                                                                                                        | Low      | Track analytics as planned feature.                                                                                                                                                      | **Fixed** (PR-4)                                    |
| F-12 | `docs/frontend/UTILS_LAYER.md:99`                                                                     | Contains `Analytics service integration (TODO - currently commented out)`                                                                                                                                                                                                                                                                                                                                                      | Low      | Same as F-11.                                                                                                                                                                            | **Fixed** (PR-4)                                    |
| F-13 | Booking status type                                                                                   | Booking `status` is VARCHAR, not a PG ENUM (unlike `user_role_enum`). Values `pending`, `confirmed`, `refund_pending`, `cancelled`, `refund_failed` are enforced only at app level.                                                                                                                                                                                                                                            | Low      | Consider creating `CREATE TYPE booking_status AS ENUM (...)` for DB-level enforcement, or document this as intentional.                                                                  | **Fixed** (PR-4, documented as intentional)         |
| F-14 | `docker-compose.yml:40`                                                                               | Redis password default `soleil_redis_secret_2026` is hardcoded in docker-compose.yml (visible in repo).                                                                                                                                                                                                                                                                                                                        | Medium   | Use env var without default, or document that this is local-dev-only and acceptable.                                                                                                     | **Fixed** (PR-1)                                    |
| F-15 | `backend/.env.test`                                                                                   | Tracked in git with `DB_CONNECTION=mysql` — contradicts PostgreSQL requirement. Contains placeholder credentials.                                                                                                                                                                                                                                                                                                              | Medium   | Remove from git or update to PostgreSQL config.                                                                                                                                          | **Fixed** (batch-2)                                 |
| F-16 | `.github/workflows/tests.yml:397,421,467,490`                                                         | Pint, Psalm, Composer Audit, NPM Audit all have `continue-on-error: true` — quality gates are non-blocking.                                                                                                                                                                                                                                                                                                                    | Medium   | Set `continue-on-error: false` for at minimum Pint and Composer Audit.                                                                                                                   | **Fixed** (batch-1)                                 |
| F-17 | `backend/.env.testing:3`                                                                              | Committed `APP_KEY=base64:...` — test-only but committed encryption keys are a hygiene issue.                                                                                                                                                                                                                                                                                                                                  | Low      | Set `APP_KEY=` (empty) and let CI generate it.                                                                                                                                           | **Fixed** (batch-2)                                 |
| F-18 | `frontend/src` (multiple files)                                                                       | 8 TODO markers including `console.log` in SearchCard.tsx production code.                                                                                                                                                                                                                                                                                                                                                      | Low      | Remove console.log; track TODOs in backlog.                                                                                                                                              | **Fixed** (batch-3)                                 |
| F-19 | `docs/DEVELOPMENT_HOOKS.md:23`                                                                        | Says "142 tests" for frontend; actual: 145 tests. Residual of F-03.                                                                                                                                                                                                                                                                                                                                                            | Low      | Update test count.                                                                                                                                                                       | **Fixed** (batch-4)                                 |
| F-20 | CI workflows                                                                                          | No `docker compose config` validation in CI despite being listed as DoD gate in agents/CONTRACT.md.                                                                                                                                                                                                                                                                                                                            | Low      | Add lightweight CI job running `docker compose config`.                                                                                                                                  | **Fixed** (batch-1)                                 |
| F-21 | `frontend/src/features/auth/LoginPage.tsx` + `RegisterPage.tsx`                                       | UI labels in English ("Welcome Back", "Create Account", etc.) instead of Vietnamese. App convention is Vietnamese for all user-facing copy.                                                                                                                                                                                                                                                                                    | Medium   | Translate all user-facing strings to Vietnamese, consistent with BookingForm/Dashboard.                                                                                                  | **Fixed**                                           |
| F-22 | `backend/app/Http/Controllers/Auth/HttpOnlyTokenController.php:290`                                   | String literal `'Token cookie không ditemukan.'` is Indonesian, not Vietnamese. All other user-facing strings in this file are Vietnamese. Discovered during TD-002 (out of scope — string literal, not a comment).                                                                                                                                                                                                            | Low      | Replace with Vietnamese equivalent: `'Token cookie không tìm thấy.'`                                                                                                                     | **Fixed**                                           |
| F-23 | `docs/COMPACT.md` (sections 1–7)                                                                      | Pre-existing Markdown lint warnings: MD022 (headings not surrounded by blank lines), MD032 (lists not surrounded by blank lines), MD031 (fenced code blocks not surrounded by blank lines) across the static sections of the file. Introduced before Phase 5 session.                                                                                                                                                          | Low      | Add blank lines between headings and their immediately-following list/code-block content. No logic change — formatting only.                                                             | Open                                                |
| F-24 | `backend/app/Http/Controllers/Auth/AuthController.php:115,233` + `HttpOnlyTokenController.php:92,199` | Auth controllers use `DB::table('personal_access_tokens')->insertGetId()` (Query Builder) to create tokens, bypassing Eloquent model events. Cannot simply switch to `PersonalAccessToken::create()` because the `HasUuids` trait generates UUID primary keys while the DB column is `bigIncrements` (integer). Requires either: (a) override `uniqueIds()` on `PersonalAccessToken` to exclude PK, or (b) migrate PK to UUID. | High     | Override `uniqueIds()` on `PersonalAccessToken` to return `['token_identifier']` instead of the primary key, then replace `DB::table()` calls with Eloquent `$user->tokens()->create()`. | **Fixed** (H-02, 2026-03-06)                        |
| F-25 | `frontend/src/shared/lib/api.ts` — 401 refresh interceptor | `refreshResponse.data.csrf_token` reads wrong level; actual path is `refreshResponse.data.data.csrf_token` (wrapped response). Non-critical: `check_httponly_token` routes do not validate CSRF so the misread value is never enforced. Identified 2026-03-14. | Low | Fix to `refreshResponse.data.data.csrf_token` in the 401 refresh-interceptor success branch. | **Fixed** (2026-06-01, frontend response-envelope fixes) — the interceptor now types the call `api.post<{ data: { csrf_token: string } }>(...)` and reads `refreshResponse.data.data.csrf_token`, so the rotated token reaches `setCsrfToken` (`api.ts` 401-refresh success branch). Regression guard: `api.test.ts` success + concurrent tests now mock the nested `{ data: { data: { csrf_token } } }` envelope and assert `setCsrfToken` is **not** called with `undefined`. |

## 2026-03-20 Audit Findings — Backend Security

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-26 | `backend/app/Services/BookingService.php:84-121` | `confirmBooking()` runs inside a `DB::transaction` without `lockForUpdate()`. Two concurrent Stripe webhooks or admin confirms can both pass the status pre-check and double-write. `CancellationService` uses the lock correctly; this method does not. | Critical | Add `Booking::query()->where('id', $booking->id)->lockForUpdate()->first()` at top of the transaction closure, re-check status on the locked row before updating. | **Fixed** (`f25273c`) — `confirmBooking()` opens its `DB::transaction` with `Booking::query()->whereKey($booking->id)->lockForUpdate()->firstOrFail()` and re-checks `paymentAllowsConfirmation()` on the locked row (`BookingService.php:111-119`). |
| F-27 | `backend/app/Http/Controllers/AdminBookingController.php:106-144` | `restore()` checks for overlapping bookings outside a transaction, then calls `bookingService->restore()` in a separate operation — a TOCTOU race. A concurrent booking creation can fill the slot between the overlap check and the restore. | Critical | Wrap both `hasOverlappingBookings` and `bookingService->restore()` in a single `DB::transaction` with `lockForUpdate()` on the target booking row. | **Fixed** (`902f912`) — TOCTOU closed inside `BookingService::restore()`: lock + `hasOverlappingBookingsWithLock` + `restoreWithAudit` now run in one `DB::transaction` (`BookingService.php:381-420`); `AdminBookingController::restore()` delegates to it (`:142-153`). |
| F-28 | `backend/app/Http/Controllers/AdminBookingController.php:194-241` | `restoreBulk()` has the same TOCTOU race as `restore()`: per-booking overlap check and restore are two separate DB operations without transaction + lock. | Critical | Wrap each iteration's check-and-restore in `DB::transaction` + `lockForUpdate()`. | **Fixed** (`902f912`) — `restoreBulk()` calls the same transactional, row-locked `BookingService::restore()` per booking (`AdminBookingController.php:234-253` → `BookingService.php:381-420`); the per-iteration check-and-restore is now atomic. |
| F-29 | `backend/app/Http/Controllers/AuthController.php:53,87` | Legacy `AuthController::register()` and `login()` call `$user->createToken()` without setting `expires_at`. Resulting tokens bypass the custom `CheckTokenNotRevokedAndNotExpired` middleware entirely — effectively immortal until manually deleted. Endpoints are deprecated but still live. | High | Set `expires_at` on the token at creation time in the legacy controller (e.g. `now()->addDays(30)`), or add a hard date gate expiring all legacy tokens on the July 2026 sunset date. | **Fixed** (`acac8ca`, H-02; token creation refactored to a shared helper since) — both `register()` (`AuthController.php:57`) and `login()` (`:103`) issue tokens through one private helper `issueLegacyBearerToken()` (`:175`), which calls `$user->tokens()->create([...])` with `'expires_at' => now()->addMinutes((int) config('sanctum.short_lived_token_expiration_minutes', 60))` (`:181`, `:189`). No `createToken()` call remains in the controller, so no legacy path can mint an immortal token. (Evidence refreshed 2026-06-02: the prior citation `:133-137`/`:264-268` + `addDays(long_lived_token_expiration_days)` was stale — the real mechanism is the short-lived-minutes helper.) |
| F-30 | `backend/routes/api.php:92-104` | `GET /api/auth/csrf-token` previously allowed anonymous callers to fetch the supplementary CSRF token. Closed by making the endpoint authenticated-only with `check_token_valid`, retaining throttling as secondary abuse control via `throttle:csrf-token`, and adding non-cacheable response headers. `/sanctum/csrf-cookie` remains the pre-auth Sanctum bootstrap route. | High | Landed: require `check_token_valid`, use named `csrf-token` limiter, keep `/sanctum/csrf-cookie` public for SPA bootstrap. | **Fixed** (2026-05-11) |
| F-31 | `backend/app/Http/Controllers/Auth/UnifiedAuthController.php:154-196` | `detectAuthMode()` uses `config('sanctum.max_refresh_count', 50)` as the suspicious-activity threshold. Every other location in the codebase uses `config('sanctum.max_refresh_count_per_hour', 10)`. The wrong key allows 50 refreshes before flagging theft — 5× the intended limit. | High | Change key to `sanctum.max_refresh_count_per_hour` with default `10`. | **Fixed** (`78aa03e`, `5e258e7`) — the wrong-key threshold was removed from `detectAuthMode()`; it now only resolves the auth mode (cookie via `token_hash`, Bearer via `findToken`) with no `max_refresh_count` read at all (`UnifiedAuthController.php:154-195`). Suspicious-refresh thresholding moved to the cache-backed refresh limiter on the correct `per_hour` key. |
| F-32 | `backend/app/Http/Controllers/Auth/UnifiedAuthController.php:154-196` | `detectAuthMode()` Bearer lookup queries `PersonalAccessToken::where('token', $tokenHash)` by hashing the full `{id}\|{token}` string. Sanctum stores only the hash of the random portion. The lookup will always return `null` — the Bearer path in `detectAuthMode()` is silently broken, returning 401 for all valid Bearer tokens on `/auth/unified/*`. | High | Replace with `PersonalAccessToken::findToken($bearerToken)` as used in `CheckTokenNotRevokedAndNotExpired`. | **Fixed** (F-32, 2026-05-01) |
| F-33 | `backend/app/Services/CancellationService.php:268-282` | `finalizeCancellation()` runs inside a `DB::transaction` without `lockForUpdate()` on the booking. Between `processRefund()` succeeding and the status update to `CANCELLED`, a concurrent request can observe `refund_pending` and re-enter the refund flow. | Medium | Add `Booking::query()->where('id', $booking->id)->lockForUpdate()->first()` at the top of the `finalizeCancellation` closure. | **Fixed** (`bd3cff9`) — `finalizeCancellation()` re-acquires the lock and re-reads fresh: `Booking::query()->whereKey($booking->getKey())->lockForUpdate()->firstOrFail()`, with an idempotent terminal-state guard (`CancellationService.php:373-385`). |
| F-34 | `backend/app/Http/Controllers/ContactController.php:43` | `$request->input('per_page', 15)` is passed directly to `getPaginated()` without validation or clamping. `per_page=100000` causes a massive un-paginated DB scan. | Medium | Clamp: `$perPage = min((int) $request->input('per_page', 15), 100)` or use a FormRequest with `integer\|min:1\|max:100`. | **Fixed** (`37e4bf0`) — `ContactController::index(ContactIndexRequest)` now reads bounded `perPage()` validation output. |
| F-35 | `backend/app/Http/Controllers/Auth/HttpOnlyTokenController.php:40-141` | `login()` accesses `$request->input('email')` and `$request->input('password')` directly without a FormRequest. Missing presence/type/length validation makes the endpoint susceptible to type-coercion edge cases (e.g. array inputs). `LoginRequest` exists but is not used here. | Medium | Inject `LoginRequest` (already exists in `app/Http/Requests/Auth/`) instead of raw `$request->input()`. | **Fixed** (`5e258e7`) — `login()` is now type-hinted `login(LoginHttpOnlyRequest $request)` (a dedicated FormRequest with presence/type/length rules), replacing raw `$request->input()` (`HttpOnlyTokenController.php:6,45`). |
| F-36 | `backend/app/Http/Controllers/Auth/HttpOnlyTokenController.php:209` + `backend/app/Http/Middleware/CheckHttpOnlyTokenValid.php:82` | Fence-post inconsistency: `refresh()` uses `>=` for the suspicious-activity threshold check; `CheckHttpOnlyTokenValid` uses `>`. A token at the exact threshold value passes middleware but is rejected on next refresh — one extra use of a token that should already be revoked. | Medium | Align both to `>=` for strict enforcement at the limit. | **Fixed** (`5e258e7`, `78aa03e`) — refresh enforcement is unified in cache-backed `TokenRefreshRateLimiter`; both token modes delegate to the same threshold implementation. |
| F-37 | `backend/app/Services/CustomerService.php:46` | `getCustomerProfile(string $email)` uses `"customer_profile_{$email}"` as a raw cache key. Emails with `:`, `{`, `}`, `(`, `)`, `/`, `\`, `@` characters can collide with or pollute other cache entries depending on driver. | Medium | Hash the email: `"customer_profile_".md5($email)`. | **Fixed** (`ba1e5d5`) — cache keys use `hash('sha256', strtolower(trim($email)))`. |
| F-38 | `backend/app/Http/Controllers/Payment/StripeWebhookController.php:144-159` | `handleChargeRefunded()` runs inside a `DB::transaction` without `lockForUpdate()` on the booking row. Low probability given Stripe delivery guarantees, but inconsistent with the project's locking discipline. | Low | Add `$booking->lockForUpdate()` or re-query with lock inside the transaction closure. | **Fixed** (`abc3959`) — `handleChargeRefunded()` re-queries the booking under a row lock inside the transaction: `Booking::where('payment_intent_id', $paymentIntentId)->lockForUpdate()->first()` (`StripeWebhookController.php:248-250`). |
| F-39 | `backend/app/Http/Middleware/CheckHttpOnlyTokenValid.php:36-46` | Manual fallback cookie parsing via `strpos`/`substr` does not `urldecode()` the extracted value. URL-encoded token identifiers (unlikely but possible) will fail hash comparison silently. | Low | Apply `urldecode()` to the extracted cookie value, or rely exclusively on `$request->cookie()` which Laravel decodes automatically. | **Fixed** (`32c6a39`) — fallback parsing now exact-matches the cookie name, splits once, and `rawurldecode()`s the value. |
| F-40 | `frontend/src/shared/lib/api.ts:158-161` | On refresh failure, code calls `localStorage.removeItem` for `auth_token` and `refresh_token`. Implies these values may be stored in `localStorage` (XSS-reachable). If any code path writes Bearer tokens to `localStorage`, this is a HIGH security issue. If no code does, the calls are dead code masking a latent risk. | Low | Audit all `localStorage.setItem` calls for auth keys. Remove the `localStorage.removeItem` calls if no code writes tokens there. | **Fixed** (verified 2026-06-02) — zero `localStorage.setItem` calls exist under `frontend`; the refresh-failure path now clears only the session-scoped `csrf_token`, preserving the HttpOnly-cookie auth contract. |
| F-41 | `backend/app/Http/Middleware/CheckTokenNotRevokedAndNotExpired.php:36-44` | When no Bearer token is present and no guard has a user, the middleware directly instantiates `CheckHttpOnlyTokenValid::handle()` — bypassing Laravel's IoC container and middleware resolution pipeline. Fragile against future constructor injection on `CheckHttpOnlyTokenValid`. | Medium | Use `app()->make(CheckHttpOnlyTokenValid::class)->handle(...)` for proper IoC resolution, or restructure the middleware stack. | **Fixed** (`fa8d6f8`) — fallback resolves `CheckHttpOnlyTokenValid` through `app(...)` before delegation. |

## 2026-03-20 Audit Findings — Database Integrity

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-42 | `backend/app/Models/Room.php:183-188` | `scopeAvailableBetween` uses `whereDoesntHave('bookings', ...)` without an explicit `whereNull('bookings.deleted_at')`. Soft-delete exclusion relies on the implicit Eloquent global scope; the dual-layer invariant requires it to be explicit. | Medium | Add `->whereNull('bookings.deleted_at')` inside the `whereDoesntHave` closure as defense-in-depth. | **Fixed** (`a8aee5b`) — `scopeAvailableBetween()` explicitly filters `bookings.deleted_at IS NULL` and uses `BookingStatus::ACTIVE_STATUSES`. |
| F-43 | `backend/app/Repositories/EloquentRoomRepository.php:82` | `hasOverlappingConfirmedBookings()` queries `status = 'confirmed'` only, missing `pending`. This is inconsistent with `Booking::ACTIVE_STATUSES` (`[pending, confirmed]`) and the DB EXCLUDE constraint — `pending` bookings are invisible to this overlap check. | Medium | Change to `->whereIn('status', ['pending', 'confirmed'])` or use `BookingStatus::activeStatuses()`. | **Fixed** (`4808142`, regression guard `fcd323f`) — renamed `hasOverlappingActiveBookings()` uses `BookingStatus::ACTIVE_STATUSES`; pending-overlap coverage is locked in by `RoomOverlapActiveStatusTest`. |
| F-44 | `backend/database/migrations/2025_11_24_000000_create_reviews_table.php:41` + `backend/app/Models/Review.php:39-41` | `reviews.approved` column has DB-level default `true` (migration), but Eloquent `$attributes` sets default `false` (model). Any raw `INSERT` bypassing the model (seeders, `DB::table`, queue jobs) silently auto-approves reviews. | Medium | Add a migration: `ALTER TABLE reviews ALTER COLUMN approved SET DEFAULT false`. | **Fixed** (`fcd323f`, SH-09) — guarded migration `2026_05_30_000001_set_reviews_approved_default_false.php` aligns the DB default with the model; regression coverage added. |
| F-45 | `backend/app/Http/Controllers/AdminBookingController.php:203-235` + `backend/app/Services/BookingService.php:391-396` | `restoreBulk()` calls `getTrashedBookingById()` in a `foreach` loop — one SELECT per booking ID (N+1). | Medium | Load all trashed bookings in a single query before the loop: `Booking::onlyTrashed()->with(['room','user'])->whereIn('id', $ids)->get()->keyBy('id')`. | **Fixed** (`a1cc270`) — `restoreBulk()` batches the trashed lookup via `BookingRepository::findTrashedByIds($ids, ['room','user'])->keyBy('id')` (single query) + id de-dup; each restore keeps its own lock/overlap/audit/event (`AdminBookingController.php:245-282`). |
| F-46 | `backend/database/migrations/2026_02_22_000002_add_fk_reviews_booking_id.php:26,41` | Uses `config('database.default') === 'sqlite'` driver guard (weaker pattern). All post-2026-03-17 migrations correctly use `DB::getDriverName() !== 'pgsql'`. Can produce false-negatives if `database.default` is overridden at runtime. | Medium | Change to `DB::getDriverName() !== 'pgsql'` to match project-wide convention. (Same applies to `2026_02_09_000000` — cleanup deferred per ARCHITECTURE_FACTS note.) | **Fixed** (`a1cc270`) — `up()`/`down()` now guard on `DB::getDriverName() !== 'pgsql'` (`2026_02_22_000002_add_fk_reviews_booking_id.php:28,42`); the separate `2026_02_09_000000` cleanup remains deferred per the ARCHITECTURE_FACTS note. |
| F-47 | `backend/database/migrations/2026_02_11_000000_reconcile_legacy_index_ordering.php:52-55` | `down()` is an empty no-op. Rolling back this migration does not remove indexes added in `up()`. Subsequent `down()` migrations attempting to `dropIndex` by name on those indexes could fail depending on order. | Medium | Implement `down()` to drop each index added in `up()`, wrapped in `hasIndex` existence checks for idempotency. | **Fixed** (`a1cc270`) — `down()` drops the two indexes this migration actually creates (`bookings_status_check_out_index`, `bookings_room_id_check_in_check_out_index`), each `hasIndex`-guarded; pre-existing indexes are intentionally left to their owning migrations (`2026_02_11_000000:52-74`). |
| F-48 | `backend/app/Models/Location.php` | `lock_version` is absent from `Location::$casts`. Reads return `lock_version` as a string. The optimistic-locking CAS query uses integer comparison — type mismatch could cause silent failures if the driver returns strings. (`Room.php` correctly casts `lock_version` to `integer`.) | Low | Add `'lock_version' => 'integer'` to `Location::$casts`. | Resolved (Wave 3 PR-3D) |
| F-49 | `backend/app/Services/BookingService.php:111` + `backend/app/Http/Controllers/BookingController.php:225` | `confirmBooking()` calls `$booking->user->notify(...)` inside a transaction without eager-loading `user`. Triggers a lazy-load query inside the lock-hold window, unnecessarily extending transaction duration. | Low | Eager-load `user` before calling `confirmBooking()`, or dispatch notification via `afterCommit` using `TransactionCommitting`. | **Fixed** (`55d68f5`) — confirmation now dispatches `SendBookingConfirmationEmail` with `afterCommit()` and no in-transaction lazy-loaded user notification. |

## 2026-03-20 Audit Findings — Frontend

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-50 | `frontend/src/shared/utils/toast.ts:1-2` + `frontend/package.json:25` | `react-toastify` is a forbidden third-party toast library (rule: no `react-hot-toast` or equivalents). `toast.ts` wraps it; `ToastContainer` is imported in `main.tsx`. The `showToast` abstraction means call-sites need no change if the library is replaced. | Critical | Replace `react-toastify` with an in-house toast implementation (React context + portal). The `showToast` API surface is already abstracted. | **Fixed** (`4323e90`, verified 2026-06-02) — `toast.ts` is an in-house React portal implementation and neither source nor the package manifest references `react-toastify`. |
| F-51 | `frontend/src/features/booking/BookingList.tsx:16-33` + `frontend/src/features/auth/AuthContext.tsx:175` + `frontend/src/features/booking/BookingForm.tsx:128` | `BookingList.tsx` uses a manual `active` flag instead of the required `AbortController` pattern and never passes a signal to `fetchMyBookings`. `AuthContext.tsx` and `BookingForm.tsx` use `as { response?: ... }` type assertions on caught errors instead of `isAxiosError()` narrowing. | Critical | Replace `active` flag with AbortController + pass `signal` to `fetchMyBookings`. Use `isAxiosError(err)` (already imported in `AuthContext.tsx`) for safe narrowing. | **Fixed** (`4323e90`, completed by `2d8ac3b`) — booking-list cancellation and error state were already landed; auth login narrowing and shared Axios-aware cancellation handling are now reconciled. |
| F-52 | `frontend/src/features/admin/rooms/RoomForm.tsx:36-69`, `frontend/src/features/admin/bookings/TodayOperations.tsx:18-23,40-43`, `frontend/src/features/admin/bookings/BookingCalendar.tsx:31-52`, `frontend/src/features/admin/bookings/AdminBookingDashboard.tsx:27-34,55-60`, `frontend/src/features/admin/customers/CustomerProfile.tsx:17-39`, `frontend/src/features/admin/customers/CustomerList.tsx:18-24` | 7 `useEffect` hooks across 5 admin components make API calls via `.then()` chains with no `AbortController` and no cleanup return. State updates fire on unmounted components; some also have `eslint-disable` comments suppressing missing-deps warnings. | High | Add `const controller = new AbortController()` to each effect, pass `{ signal: controller.signal }` to each `api.get()` call, and return `() => controller.abort()`. Remove `eslint-disable` comments. | **Fixed** (`2d8ac3b`) — customer admin API functions accept signals, all listed admin effects abort on cleanup, and cancellation checks use the shared Axios-aware helper. |
| F-53 | `frontend/src/features/admin/bookings/adminBooking.api.ts:70-84` | `getTodayArrivals()` and `getTodayDepartures()` return `response.data.data` typed as `BookingDetailRaw[]`, but the admin bookings endpoint wraps in `{ bookings, meta }`. The correct expression is `response.data.data.bookings`. This type mismatch silently returns `undefined` at runtime. | Medium | Fix return expression to `response.data.data.bookings` and align the return type. | **Fixed** — already correct in HEAD (phantom-Open): `getTodayArrivals()`/`getTodayDepartures()` read `response.data.data.bookings.map(parseBookingStatusPayload)` (`adminBooking.api.ts:102,119`) and are typed `Promise<BookingDetailRaw[]>`. Now locked in by `__tests__/adminBooking.api.test.ts`, which asserts each returns a real array (not `undefined`) from a `{ data: { data: { bookings, meta } } }` fixture. |
| F-54 | `frontend/src/features/admin/bookings/BookingCalendar.tsx:46-51`, `frontend/src/features/admin/rooms/RoomForm.tsx:38`, `frontend/src/features/admin/rooms/AdminRoomDashboard.tsx:26-29`, `frontend/src/features/admin/bookings/TodayOperations.tsx:19`, `frontend/src/features/admin/bookings/AdminBookingDashboard.tsx:28-33` | 5 components call `api.get('/v1/locations')` directly instead of going through a dedicated API module function. Rule: all API calls must go through named functions in `*.api.ts` files; `getLocations()` already exists in `@/shared/lib/location.api`. | Medium | Replace all direct `api.get('/v1/locations')` calls with `getLocations()` from `@/shared/lib/location.api`. For rooms, use `getRoomsByLocation()` from `adminRoom.api.ts`. | **Fixed** (`2d8ac3b`) — the final direct locations call in `AdminBookingDashboard` now uses `getLocations()`; guard grep finds no direct `/v1/locations` client calls. |
| F-55 | `frontend/src/features/admin/bookings/BookingCalendar.tsx:175` | `window.location.href = \`/admin/bookings/${booking.id}\`` causes a full page reload. Should use React Router `useNavigate`. | Medium | Replace with `navigate(\`/admin/bookings/${booking.id}\`)` using `useNavigate()`. | **Fixed** (2026-06-01) — the booking-cell `onClick` now calls `navigate('/admin/bookings/' + booking.id)` from `useNavigate()` instead of assigning `window.location.href` (`BookingCalendar.tsx`), so the SPA no longer hard-reloads. Test: `BookingCalendar.test.tsx` "navigates via React Router (not window.location)". |
| F-56 | `frontend/src/features/admin/bookings/AdminBookingTable.tsx:31` | `window.prompt()` used for collecting cancellation reason — a blocking browser-native dialog, inconsistent with the rest of the UI which uses `ConfirmDialog`. | Medium | Replace with a custom modal/dialog component using React state, consistent with `ConfirmDialog` used elsewhere. | **Fixed** (verified 2026-06-02) — the cited `window.prompt()` no longer exists anywhere in `frontend/src` (grep: zero `window.prompt`/`prompt(`); `AdminBookingTable.tsx` is now a presentational table with no cancellation-reason capture (no `prompt`/`useState`/dialog). The removed inline cancel-with-reason UI left `adminCancelBooking(id, reason)` orphaned; the zero-caller wrapper has now been deleted from `adminBooking.api.ts`. |
| F-57 | `frontend/src/features/home/components/SearchCard.test.tsx:9-13`, `frontend/src/features/booking/BookingForm.test.tsx:55-58`, `frontend/src/features/bookings/GuestDashboard.test.tsx:11-12` | `mockNavigate` and `mockCreateBooking` are declared as module-level `vi.fn()` variables captured by `vi.mock()` factory closures — the exact hoisting failure pattern in Vitest 2.x jsdom. Will cause flaky CI failures on a Vitest upgrade. | Medium | Move affected mocks into `vi.hoisted()`: `const { mockNavigate } = vi.hoisted(() => ({ mockNavigate: vi.fn() }))`. | **Fixed** (verified 2026-06-02) — all three cited test modules declare mock-factory dependencies through `vi.hoisted()`; focused Vitest PASS (56). |
| F-58 | `frontend/src/app/router.tsx:88`, `frontend/src/features/auth/ProtectedRoute.tsx:34`, `frontend/src/features/admin/rooms/RoomForm.tsx:109` | Three English UI strings in user-facing code: `"Loading..."` (Suspense fallback), `"Checking authentication..."` (ProtectedRoute), `"Version conflict. Please refresh."` (RoomForm error). All user-facing text must be Vietnamese. | Medium | Change to `"Đang tải..."`, `"Đang xác thực..."`, `"Xung đột phiên bản. Vui lòng tải lại trang."` respectively. | **Fixed** (`2d8ac3b`, verified 2026-06-02) — the cited strings were already translated; remaining room-classification and today-operations labels were translated in the Track 4 sweep. |
| F-59 | `frontend/src/features/booking/BookingList.tsx` | No error state is tracked. If `fetchMyBookings` fails, the component silently stays in empty state with `isLoading: false`. Inconsistent with `GuestDashboard` and `RoomList` which surface errors to the user. | Medium | Add `const [error, setError] = useState<string \| null>(null)` and render it in the UI. | **Fixed** (`4323e90`, verified 2026-06-02) — `BookingList` renders an explicit Vietnamese error state when loading fails. |
| F-60 | `frontend/src/features/admin/bookings/adminBooking.api.ts:4-12` | `AdminBookingsResponse` is declared in this file (shape: `{ bookings, meta }`) and also in `admin.types.ts` (shape: `{ data: { bookings, meta } }`). Two conflicting types with the same name in the same feature. | Low | Remove the duplicate from `adminBooking.api.ts` and import from `admin.types.ts`, or rename one clearly. | **Fixed** (verified 2026-06-02) — `adminBooking.api.ts` imports the canonical envelope from `admin.types.ts` and keeps its wire-status validation overlay local. |
| F-61 | `frontend/src/features/admin/bookings/TodayOperations.tsx:49-50` | `lock_version: 1` is hardcoded with comment `/* ignoring lock check for brevity */`. This bypasses the optimistic locking contract. | Low | Remove the hardcoded value and properly read `lock_version` from the room/booking entity, or formally defer with a tracked backlog item. | **Fixed** (`fcd323f`, verified 2026-06-02) — today operations uses the canonical readiness endpoint and sends no hardcoded `lock_version`; regression coverage asserts its absence. |
| F-62 | `frontend/src/features/locations/LocationDetail.tsx:312` | Room price displayed as `$${room.price}/night` (dollar sign, English "night"). All other price displays use `formatVND` and Vietnamese strings. | Medium | Replace with `{formatVND(room.price)}<span>/đêm</span>`. | **Fixed** (`4657ff2`, verified 2026-06-02) — location detail renders `formatVND(...)` with `/đêm`. |

## 2026-04-05 Audit Findings — Full-Stack Scan (2c-A through 2c-F)

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-63 | `frontend/src/features/admin/bookings/TodayOperations.tsx:49,66` | `api.patch("/v1/rooms/{roomId}/status", {...})` calls a backend route that does not exist. `backend/routes/api/v1.php` has no `PATCH /rooms/{id}/status` route. `adminRoom.api.ts:38-39` explicitly TODOs this as `[XL-CONTRACT-02]`. Any user reaching the check-in/check-out action in TodayOperations triggers a 404. | High | Implement `PATCH /api/v1/rooms/{id}/status` on the backend (controller + route + FormRequest) or remove the feature from TodayOperations.tsx until the backend is ready. Tag: XL-CONTRACT-02. | **Fixed** (`fcd323f`, SH-10) — `PATCH /api/v1/rooms/{room}/readiness` is implemented with `UpdateRoomReadinessRequest`, `RoomPolicy::updateReadiness`, moderator routing, and frontend `updateRoomReadiness()`. |
| F-64 | `backend/app/Providers/AppServiceProvider.php` + `backend/app/Providers/AuthServiceProvider.php` | Both providers register `Booking::class => BookingPolicy::class` and `Room::class => RoomPolicy::class` via `$policies`. `AuthServiceProvider` is the canonical location (also holds Review policy and all Gate definitions). The duplicate in `AppServiceProvider` is a maintainability hazard: future divergence silently picks whichever provider boots last. | Medium | Remove the `$policies` array from `AppServiceProvider` entirely, leaving `AuthServiceProvider` as the single source of truth for policy registration. | **Fixed** (`89e42b8`) — duplicate `$policies` declaration removed from `AppServiceProvider`; `AuthServiceProvider` remains authoritative. |
| F-65 | `backend/.env.example:67,85` | `MAIL_HOST` is declared twice: `smtp.mailtrap.io` (line 67) then `127.0.0.1` (line 85). Last value wins in .env parsing — effective host is `127.0.0.1`. Developers configuring mailtrap credentials will silently connect to localhost. | Low | Remove the duplicate mail block (lines 83-90). Consolidate into a single mail configuration section with clear dev vs. production comments. | **Fixed** (`99eab5a`) — `.env.example` contains one consolidated mail block. |
| F-66 | `frontend/src/features/auth/AuthContext.tsx:43` | JSDoc comment states "CSRF protection via X-XSRF-TOKEN header validation". `CheckHttpOnlyTokenValid.php` explicitly does NOT validate X-XSRF-TOKEN; the primary CSRF control is `SameSite=Strict` on the cookie. The comment overstates server-side enforcement. | Low | Correct to: "CSRF protection via SameSite=Strict cookie; X-XSRF-TOKEN header is sent as defense-in-depth but is not validated server-side." | **Fixed** (`99eab5a`, expanded `4108e47`) — `AuthContext` documents SameSite Strict as primary and `X-XSRF-TOKEN` as defense-in-depth only. |

## 2026-04-18 Audit Findings — AI Harness Security

Follow-up audit on AI harness Phase 4 proposal confirmation flow. Scope: cross-user hash confirmation vector (previously tracked as T-13 "Accepted" in THREAT_MODEL_AI.md).

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-67 | `backend/app/Http/Controllers/ProposalConfirmationController.php:55-62` + `backend/app/AiHarness/Services/AiOrchestrationService.php` | Proposer-binding absence: the AI proposal cache envelope did not carry `proposer_user_id`. Any authenticated user who obtained a proposal hash (via side channel or — theoretically — brute force of the 64-char SHA-256) could call `POST /api/v1/ai/proposals/{hash}/decide` and trigger a real booking write or cancellation under their own account while impersonating the original proposer's intent. Closed T-13 "Proposal ownership bypass" (previously Accepted). Initially cited as "F-06 (2026-04-18)"; promoted to F-67 on 2026-04-19 to disambiguate from 2026-02-21 F-06 (CHECK constraint). | Critical | (Landed) Producer (`AiOrchestrationService`) writes `proposer_user_id` into the cache payload. Consumer (`ProposalConfirmationController::decide`) reads the field and returns **404** (not 403) when it is absent or does not equal `(int) $request->user()->id`. Legacy pre-F-67 cache entries are treated as unbound → 404. Service-layer `CancellationService::validateCancellation` ownership gate provides defense-in-depth for alternate callers. Commits `17a4880`, `39cba7a`. | **Fixed — Mitigated** (2026-04-18, T-13 reclassified) |

## 2026-04-19 Audit Findings — Local Test Suite Reliability

Discovered during pre-push verification of the docs-governance commit batch (HEAD `9261798`). Not a production-code issue; surfaces only during `php artisan test` on the local PostgreSQL test DB (`soleil_test`).

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-68 | `backend/database/migrations/2026_02_09_000005_assign_rooms_to_locations.php:50` (+ `:58` in `down()`) | `$table->unsignedBigInteger('location_id')->nullable(false)->change()` invokes doctrine/dbal, which opens a *second* PostgreSQL connection to inspect the current column type before emitting `ALTER TABLE rooms ALTER COLUMN location_id TYPE bigint, ALTER COLUMN location_id SET NOT NULL`. The migration's step 2 (`UPDATE bookings SET location_id = r.location_id FROM rooms r …` at `:27`) runs in the migrator's primary connection and holds `RowExclusiveLock` on `rooms` and `bookings` until transaction commit. The doctrine second-connection path then races with the primary connection for `AccessExclusiveLock` on `rooms`, producing `SQLSTATE[40P01]: Deadlock detected` intermittently during `migrate:fresh` (and hence `RefreshDatabase` in tests). Reproduced 2026-04-19 running `php artisan test --filter=GateTest` against a freshly-started `soleil-hostel-db-1` container: 2 failed / 12 passed, with both failures reporting the same deadlock on this migration. Full `php artisan test` can additionally exceed the 900000 ms pre-push hook timeout on slow dev machines, which compounds the symptom but is orthogonal. CI has historically passed (954 tests), suggesting the race is timing-sensitive and the CI runner's tighter resource profile avoids it. | Medium | Replace the doctrine-routed `->change()` with a raw single-statement ALTER that does not require schema introspection: `DB::statement('ALTER TABLE rooms ALTER COLUMN location_id SET NOT NULL')` (column type is already `bigint` from migration `2026_02_09_000002:25`). Same treatment for the `down()` at `:58` using `DROP NOT NULL`. Alternatively, split the migration into two: one that completes all `DB::table()->update()` / `DB::statement('UPDATE …')` calls and commits, and a separate migration that performs the NOT NULL transition. Either approach removes the cross-connection lock race. Non-goal: adding `LOCK TABLE rooms IN ACCESS EXCLUSIVE MODE` — that masks the symptom without fixing the root cause. | **Fixed** (Batch 4 / 3I — verified, see closure note below) |

## Batch 4 closure notes (2026-04-29)

### F-68 / Phase 3I — April 19 migration deadlock — closed

Verified in HEAD: `backend/database/migrations/2026_02_09_000005_assign_rooms_to_locations.php` lines 65-71 already replaced the doctrine-routed `->change()` with a raw single-statement `DB::statement('ALTER TABLE rooms ALTER COLUMN location_id SET NOT NULL')` for the pgsql driver, exactly the suggested fix. The `down()` at line 79 uses the matching `DROP NOT NULL`. Non-pgsql drivers retain `->change()` since the lock race is PostgreSQL-specific. The deadlock is no longer reproducible from the current schema state; per Batch 4 hard-constraints, no speculative additional patch is being applied. Closing F-68 as Fixed.

### Phase 3G — Moderator-denial coverage — already complete

The Phase 1 audit checked whether moderator role has explicit 403/deny tests against the four destructive admin booking routes. Findings:

- `POST /api/v1/admin/bookings/{id}/restore` — covered by `tests/Feature/Booking/RestoreIntegrityTest.php::test_moderator_cannot_restore_booking` (line 531), `tests/Feature/Booking/AdminBookingCoverageTest.php::test_moderator_cannot_restore_booking` (line 141), and `tests/Feature/Booking/BookingSoftDeleteTest.php::test_moderator_cannot_restore` (line 479).
- `POST /api/v1/admin/bookings/restore-bulk` — covered by `RestoreIntegrityTest.php::test_moderator_cannot_bulk_restore` (line 544) and `AdminBookingCoverageTest.php::test_moderator_cannot_bulk_restore_bookings` (line 175).
- `DELETE /api/v1/admin/bookings/{id}/force` — covered by `AdminBookingCoverageTest.php::test_moderator_cannot_force_delete_booking` (line 163) and `BookingSoftDeleteTest.php::test_moderator_cannot_force_delete` (line 489).
- `GET /api/v1/admin/bookings/trashed` and `GET /api/v1/admin/bookings/trashed/{id}` — intentionally moderator-allowed per the RBAC matrix in `docs/PERMISSION_MATRIX.md`; the `v1.php` route group puts `role:moderator` on the read-only admin booking index/trashed routes. A "deny" test would contradict the matrix.

No new tests written. Adding redundant coverage was rejected per the "do not duplicate" rule in CONTRACT.md.

## 2026-05-09 Audit Findings — Harness/Security Layer

External harness audit (third-party report). Most claims were stale or unverifiable (see verification notes in WORKLOG). One genuine, in-scope item below.

| ID   | File:Line | Issue | Severity | Suggested Fix | Status |
| ---- | --------- | ----- | -------- | ------------- | ------ |
| F-69 | `backend/app/Services/RateLimitService.php`, `backend/app/Http/Middleware/AdvancedRateLimitMiddleware.php`, `backend/app/Events/RateLimiterDegraded.php`, `backend/app/Events/RequestThrottled.php`, `backend/tests/Unit/RateLimiting/AdvancedRateLimitServiceTest.php`, `backend/tests/Feature/RateLimiting/AdvancedRateLimitServiceTest.php`, `backend/tests/Feature/RateLimiting/AdvancedRateLimitMiddlewareTest.php` | Self-deprecated `RateLimitService` and its `AdvancedRateLimitMiddleware`, plus the two events they alone dispatch (`RateLimiterDegraded`, `RequestThrottled`) and three test files. The service docblock at `RateLimitService.php:25-27` already declared "Not registered in middleware stack ... Both are dead code retained for reference. Remove both files when ready to clean up." Production rate limiting is unaffected — `App\Providers\RateLimiterServiceProvider` (Laravel `RateLimiter` facade, configured for `login`, `booking`, `api-public`, `refresh-token`, `global-api`, `password-reset`, `email-verification`) remains the live path. Test suite for the dead middleware was running against `/api/health` and providing false coverage. | Medium | Delete the seven listed files; remove four stale entries from `phpstan-baseline.neon` (`AdvancedRateLimitMiddleware.php` line 97 area, three `RateLimitService.php` Redis::eval entries). | **Fixed** (2026-05-09) |

## 2026-05-19 Audit Findings — Booking Mass-Assignment Surface

Local audit pass on Booking write paths after the NEW-7 Psalm/PHPStan cleanup. Scope: `Booking::$fillable` coverage vs. controller mass-assign discipline. ID prefix `A-` (audit) is preserved from the source report; ordering is independent of the `F-` series above.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| A-1 | `backend/app/Models/Booking.php:28-56` (pre-fix) | `$fillable` historically included the full state-machine + audit + payment/deposit/refund surface (`status`, `user_id`, `deleted_by`, `payment_intent_id`, `amount`, `deposit_*`, `refund_*`, `cancelled_*`, `cancellation_reason` — 24 columns total). No live exploit existed — every controller used `$request->validated()` and FormRequest classes (`StoreBookingRequest`, `UpdateBookingRequest`) strip protected keys before the service sees them — but the regression surface was wide: any future refactor to `$request->all()`, a new controller bypass, or trust-by-default in `Booking::create` would silently let mass assignment promote a pending booking to confirmed, spoof a cancellation actor, or rewrite refund state. | Low (defense-in-depth) | Shrink `$fillable` to user-input only (`room_id`, `check_in`, `check_out`, `guest_name`, `guest_email`). Server-controlled writes (state-machine, authorship, payment, refund, cancellation audit) move to `forceFill(...)->save()` or direct property assignment in trusted service layers (`Booking::transitionTo`, `CreateBookingService`, `CancellationService`, `ReconcileRefundsJob`, `ExpireStaleBookings`, `StripeWebhookController::handleChargeRefunded`, `DevRolePreviewSeeder`). Add `BookingFillableTest` regression asserting `Booking::create(['status' => 'confirmed', 'refund_id' => 're_evil', 'cancelled_by' => 1, ...])` from user-shaped input does not persist any protected column. Tests pre-seeding protected columns via `Booking::create([...])` switch to `Booking::forceCreate(...)`. | **Fixed** (2026-05-19, commit `d67b13f`) |

## 2026-05-20 Audit Findings — Stripe Webhook Reaper Observability

Audit pass on the `webhook:reconcile-stuck-events` reaper claim loop. ID prefix `A-` (audit) continues the series above; ordering is independent of the `F-` series.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| A-2 | `backend/app/Console/Commands/ReconcileStuckStripeWebhookEvents.php:120-149` + `backend/app/Models/StripeWebhookEvent.php:105-111` (pre-fix) | `reconcile_attempts` was bumped on every claim but never gated re-claim. A row that keeps deferring (persistent transient Stripe error, network blackhole, misconfigured PaymentIntent) was re-claimed every 5-minute scheduler tick forever with no operator surface. No data-integrity impact (the booking is never wrongly mutated) — purely an observability/operational gap. | Low (observability) | Add `reconcile_attempts < config('booking.reconciliation.webhook_max_attempts')` to `scopeStaleProcessing`; add `scopeReconciliationExhausted` + `markReconciliationExhausted`; run a `failExhaustedEvents` sweep before the claim that auto-marks exhausted rows `failed` (preserving the last `error` for forensics) and emits `stripe_webhook_reconciler.reconciliation_exhausted` for log/SIEM alerting. Used a **dedicated** config key (`webhook_max_attempts`, default 12, env `BOOKING_WEBHOOK_RECONCILE_MAX_ATTEMPTS`) rather than the refund job's `reconciliation.max_attempts` (default 5) to avoid coupling the two jobs. 3 new tests cover the gate boundary, auto-fail, and full deferring→exhausted lifecycle. | **Fixed** (`4d4f823`; telemetry follow-up `c2935cf`) — `scopeStaleProcessing` now gates on `reconcile_attempts < webhook_max_attempts`, `failExhaustedEvents` auto-fails exhausted rows and emits `stripe_webhook_reconciler.reconciliation_exhausted` (`ReconcileStuckStripeWebhookEvents.php`, `StripeWebhookEvent.php`). Verified committed 2026-06-02 (`git log -S scopeReconciliationExhausted` → `4d4f823`; working tree clean) — prior "not yet committed" status was stale. |

## 2026-05-21 Findings — E2E Playwright Harness Drift

Surfaced while fixing the `@smoke` `payment-webhook` flow, which called a nonexistent `POST /api/v1/__e2e/seed-pending-booking` seed route (404 → `bookingResp.ok()` false → hard PR-gate failure). That spec was rewritten to seed through the real v1 booking API (login-v2 Bearer → pick an available room → `POST /v1/bookings` → derive the deterministic testing-fake PaymentIntent id → signed webhook → verify `confirmed` via the owner's `GET /v1/bookings/{id}`). The same "seed/login via a backend test-harness that was never implemented" assumption affects a sibling flow and the suite README.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-70 | `frontend/tests/e2e/flows/admin-restore.spec.ts:18,26` | Admin-restore (runs in the `full` nightly/dispatch job — not `@smoke`, not skipped) POSTs to `/api/v1/__e2e/seed-trashed-booking` and navigates to `/test/login-as-admin`. Neither exists: `backend/routes/api/v1.php` has no `__e2e` group, and the SPA router (`frontend/src/app/router.tsx`) has no `/test/login-as-admin` route (grep: zero matches). The seed 404s → `expect(seedResp.ok())` fails, so the nightly full-suite job fails on this flow. (Related: `ai-proposal.spec.ts` is `test.skip` unless `E2E_AI_HARNESS_ENABLED=true`, which CI never sets, so it does not fail today — but as written it never authenticates the verified guest its preconditions require and would fail if the flag were enabled.) | Medium (CI reliability; nightly only, no production impact) | Rewrite like payment-webhook: log in as seeded `admin@soleil.test` (Bearer), create a confirmed booking + soft-delete it via the real admin endpoints, and authenticate the page through the real `/login` UI instead of `/test/login-as-admin`. Or de-scope the flow from the nightly suite until rebuilt. | **Fixed** (2026-05-21) — `admin-restore.spec.ts` + `AdminDashboardPage` rebuilt to seed via the real API (admin `login-v2` → create → soft-delete) and drive the real `/admin` UI (`role=tab` labels, `ĐP #{id}` rows, `Khôi phục`); passes live. |
| F-71 | `frontend/tests/e2e/README.md:41-43` (+ flow table line 11) | README says flows are "gated behind `workflow_dispatch` until the suite stabilises" and should later be "promote[d] to `pull_request` trigger", but `.github/workflows/e2e.yml` already runs the `@smoke` flows (guest-booking + payment-webhook) on `pull_request` to `main`/`dev` as a hard merge gate. The flow-table cell for payment-webhook also still says "admin dashboard shows `confirmed`", which the rewritten spec no longer does (it verifies via the owner booking API). | Low (doc inaccuracy) | Update the README "CI" section to state guest-booking + payment-webhook are PR-required `@smoke` gates and the rest run nightly/dispatch; change the payment-webhook flow description to "→ booking API reports `confirmed`". | **Fixed** (2026-05-21) — README CI section + flow table updated. |

## 2026-05-21 Findings — Booking Soft-Delete 500 (mail notification crash)

Found by running the rebuilt admin-restore flow against a live backend (isolated `soleil_test` DB, `APP_ENV=testing`). `DELETE /api/v1/bookings/{id}` returns **500** — reproduced via curl/Node, `laravel.log` shows `ErrorException: Attempt to read property "view" on null` at `vendor/laravel/.../Notifications/Channels/MailChannel.php:96`. This is a real product bug independent of the e2e tests, and it blocks admin-restore in CI as well.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-72 | `backend/app/Http/Controllers/BookingController.php:248` → `backend/app/Listeners/SendBookingCancellation.php:41-42` → `backend/app/Notifications/BookingCancelled.php:73-83` | `destroy()` (soft delete) fires `BookingDeleted` → `SendBookingCancellation` routes an on-demand mail notification via `Notification::route('mail', $email)->notify(new BookingCancelled(...))`. On a **soft delete the booking status is not `CANCELLED`**, so `BookingCancelled::toMail()` hits its idempotency guard and returns `null`. Laravel's `MailChannel`, given an on-demand mail route + a `null` message, does not short-circuit — it proceeds to `buildView(null)` → "Attempt to read property view on null" → 500. Because `.env.example` (and the CI e2e bootstrap) use `QUEUE_CONNECTION=sync`, the `ShouldQueue` notification runs **inline during the request**, so the 500 reaches the HTTP response. Net: soft-deleting any non-cancelled booking 500s whenever `booking.notifications.send_cancellation_email` is on (default true) and the queue is sync. Hidden from the suite because Feature tests `Notification::fake()`. Blocks the admin-restore e2e flow (its seed soft-deletes a booking) in CI, not just locally. | High (core admin booking-delete endpoint 500s; data not soft-deleted because the listener throws before `softDelete` commits) | Soft delete ≠ cancellation: in `SendBookingCancellation::handle`, only send `BookingCancelled` when the booking is actually cancelled (e.g. early-return unless `$booking->status === BookingStatus::CANCELLED`, or branch so `BookingDeleted` does not send the cancellation email). Defense-in-depth: guard the on-demand send so a `null` `toMail()` never reaches `MailChannel` (skip `notify()` when `toMail` would return null). Add a Feature test for `DELETE /v1/bookings/{id}` that does **not** fake notifications (asserts 200 + soft-deleted), so the regression can't reappear. | **Fixed** (2026-05-21) — guard added in `SendBookingCancellation::handle` (skip unless `status === CANCELLED`); `BookingNotificationListenerTest` updated + regression test added (61 booking tests pass); verified live (DELETE → 200, booking soft-deleted). |

## 2026-05-21 Findings — OpenAPI `refund_status` Enum Value Drift

Surfaced while fixing the Spectral contract-lint crash (`Cannot read properties of null (reading 'enum')`, exit 2). The crash itself — a `null` member in the `type: string` enum at `docs/api/openapi.yaml:208` — was fixed in scope (removed `null`; `nullable: true` retained; verified the spec lints clean, 0 errors, against a fresh `@stoplight/spectral-cli@6` install that reproduced the crash). The finding below is the *remaining* drift in the enum's string values, which is a contract-correctness decision out of scope for the crash fix.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-73 | `docs/api/openapi.yaml:208` (`components.schemas.Booking.properties.refund_status.enum`) | After the crash fix the enum is `[pending, completed, failed]`, but `completed` is **never written** by the runtime. `CancellationService.php:351` writes `'succeeded'` (or `null`), `:380` writes `'failed'`; `ReconcileRefundsJob.php:210,276,343` write `'succeeded'`, `:225` writes `'failed'`; the migration comment at `2026_01_11_000001_add_payment_fields_to_bookings.php:43` documents `pending\|succeeded\|failed`. Worse, `StripeWebhookController.php:220,277` persists the **raw Stripe refund status** (`$latestRefund['status'] ?? 'unknown'`) verbatim, so the column can also hold `requires_action`, `canceled`, or the `'unknown'` fallback — none of which the enum lists. Net: the documented enum is both wrong (`completed` ≠ runtime `succeeded`) and incomplete (missing the webhook passthrough values). No runtime bug — `BookingResource.php:50-53` omits the field when null and otherwise serializes the stored string as-is — but a consumer doing strict response validation against the spec would reject legitimate `succeeded`/`requires_action`/`canceled`/`unknown` values. | Medium (contract inaccuracy; can break strict consumer-side response validation) | Decide one of: (a) correct the enum to the values actually persisted today (`pending`, `succeeded`, `failed`) and **normalize the webhook** to map raw Stripe statuses onto that closed set before `forceFill` (so the column can never hold `requires_action`/`canceled`/`unknown`); or (b) keep the column a passthrough and enumerate the full Stripe refund-status set + `unknown` in the spec; or (c) drop the `enum` entirely and document `refund_status` as a free-form passthrough of the Stripe refund status string. Option (a) is preferred — it makes the runtime match a small, meaningful contract rather than widening the contract to match leaky internals. Whichever is chosen, also align the migration comment. | **Fixed** (`fcd323f`) — added closed `RefundStatus` projection, fail-closed `tryFromStripe()` normalization before persistence, reconciler alignment, OpenAPI enum correction, and contract tests. |

## 2026-05-26 Findings — Refund Ledger Coverage Gap (adjacent to PAY-04)

Surfaced while fixing **PAY-04** (`ReconcileRefundsJob` now upserts `stripe_refund_events` on every refund it discovers/issues, via `App\Services\Payment\StripeRefundEventRecorder`). PAY-04 closes the ledger gap for refunds that pass through the reconciler's `REFUND_PENDING`/`REFUND_FAILED` recovery queries. The finding below is the *remaining* ledger-completeness gap on the synchronous happy path — out of PAY-04 scope (different code path, not the reconciler).

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-74 | `backend/app/Services/CancellationService.php:345-382` (`finalizeCancellation`) + `backend/app/Jobs/ReconcileRefundsJob.php:73-117` (`reconcilePendingRefunds`/`retryFailedRefunds` query predicates) | Synchronous cancellation issues the Stripe refund and `finalizeCancellation` writes the booking refund projection (`refund_id`, `refund_status='succeeded'`, `refund_amount`) and transitions the booking straight to `CANCELLED`, **without** writing a `stripe_refund_events` ledger row — by design it relies on the `charge.refunded` webhook to record the ledger (`processRefund` docblock, `:265`). If that webhook is **missed**, the booking is already terminal `CANCELLED`, which matches **neither** reconciler query (`reconcilePendingRefunds` → `status = REFUND_PENDING`, `retryFailedRefunds` → `status = REFUND_FAILED`). The reconciler therefore never revisits it, so the refund has a booking projection but **no ledger row, permanently** — the same incompleteness class PAY-04 fixed for the reconciler, but on the path PAY-04 does not cover. `bookings.refund_id` is only a latest-pointer (ARCHITECTURE_FACTS §bookings.refund_id semantics), so refund history/total-refunded/full-refund detection for these bookings is silently wrong. No double-refund or status corruption — purely ledger completeness. | Medium (audit/ledger completeness; financial reporting reads `stripe_refund_events`) | Either (a) have `CancellationService::finalizeCancellation` write the ledger row through `StripeRefundEventRecorder` inside its existing `DB::transaction` (source key e.g. `cancellation:refund:{id}`), transactionally coupling projection + ledger on the happy path exactly as the reconciler now does; or (b) widen a reconciliation sweep to detect `CANCELLED` bookings that carry a `refund_id` but have no matching `stripe_refund_events` row and backfill them. Option (a) is preferred (closes the window at write time rather than relying on a later sweep). Reuse the existing recorder; do not change refund amount/status semantics. | **Fixed** (`6d15690`) — option (a): `CancellationService::finalizeCancellation` writes the `stripe_refund_events` row via `StripeRefundEventRecorder::record()` inside its existing `DB::transaction`, source key `cancellation:refund:{refund_id}` (`CancellationService.php:403-409`), so projection + ledger are transactionally coupled on the happy path. Idempotent against the webhook/reconciler via `UNIQUE(stripe_refund_id)`. Tests: `BookingCancellationTest::test_sync_cancellation_writes_refund_ledger_row_without_webhook` + the late-webhook convergence test. |

## 2026-05-27 Findings — Relation Docblock Dual-Tool Fragility (adjacent to the PAY-03 Psalm fix)

Surfaced while fixing the PAY-03 `PaymentCancellationTask::booking` Psalm CI failure (`InvalidDocblock: Unexpected space`, psalm.dev/008). Root cause there: a relation generic annotated `@return BelongsTo<Booking, $this>` — Psalm 6.15's docblock type tokenizer trips on `$this` **preceded by a space** inside a generic param. Fixed by adopting the repo's dual-tool triple-annotation pattern (`@return …$this` + `@phpstan-return …$this` + `@psalm-return …static`). This finding records the two *other* relation methods that share the same fragility class but pass CI today only by accident of formatting (no space after the comma). ID prefix `F-` continues the series above.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-75 | `backend/app/Models/Booking.php:95` (`user()`) + `backend/app/Models/Location.php:77` (`rooms()`) | Both relation methods annotate the declaring-model generic with the **no-space** `$this` form (`@return BelongsTo<User,$this>` / `@return HasMany<Room,$this>`) and carry no `@psalm-return` override. They pass Psalm today **only because there is no space after the comma** — Psalm 6.15 emits `InvalidDocblock: Unexpected space` for `$this` preceded by a space inside a generic param (the exact failure just fixed on `PaymentCancellationTask::booking`). Any future edit that inserts a space (manual touch-up, IDE reformat, docblock-normalizing tool) reproduces the CI break. The escape hatch is non-trivial: PHPStan/Larastan **requires** `$this` here — `belongsTo`/`hasMany` return a `$this`-templated type and `TDeclaringModel` is **invariant**, so switching the single `@return` to `self`/`static` green-lights Psalm but **fails PHPStan** with a `return.type` invariance error (empirically confirmed during the PAY-03 fix). The robust dual-tool pattern already exists in-repo (`AiProposal::user/room`, `AiProposalEvent::user`). Net: two divergent conventions coexist and these two methods are on the fragile side. No runtime/user impact — docblocks are comments; purely CI/static-analysis fragility. | Low (latent CI-only; no production impact; requires a whitespace edit to trigger) | Normalize `Booking::user()` and `Location::rooms()` to the triple-annotation pattern used by `AiProposalEvent`: keep `@return …$this` (IDEs), add `@phpstan-return …$this` (PHPStan needs `$this`) and `@psalm-return …static` (Psalm parses `static` cleanly). Verify both gates stay green: `php -d opcache.enable_cli=0 vendor/bin/psalm --no-cache` **and** `vendor/bin/phpstan analyse`. Do **not** switch the single `@return` to `self`/`static` — that fails PHPStan. Optional hardening: add a CI grep guard rejecting bare (override-less) `$this` relation generics so the fragile form can't regress. | **Fixed** (`7cc563d`) — `Booking::user()` and `Location::rooms()` now use the dual-tool triple annotation (`@return $this` + `@phpstan-return $this` + `@psalm-return static`), matching `AiProposalEvent::user` (`Booking.php:106-108`, `Location.php:79-81`). |

## 2026-05-27 Findings — Synchronous Cancellation Refund Idempotency Gap (adjacent to PAY-01)

Surfaced while fixing **PAY-01** (`ReconcileRefundsJob`'s retry path now issues refunds through `StripeService::createReconciliationRefund` with a stable idempotency key + an existing-refund pre-check, so the async reconciler can no longer double-refund). The finding below is the *same* duplicate-refund class on the **synchronous** cancellation path, which PAY-01 does not touch — out of scope (different code path; PAY-01 was scoped to the reconciler job).

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-76 | `backend/app/Services/CancellationService.php:282-284` (`processRefund`, user branch) | The synchronous cancellation refund has **two divergent branches**: the no-user branch calls `StripeService::createBookingRefund` (stable idempotency key `booking:{id}:refund:{pi}` via `bookingRefundIdempotencyKey` — safe), but the **user branch calls Cashier's `$user->refund($paymentIntentId, ['amount' => $refundAmount])` with no idempotency key and no existing-refund pre-check**. `Cashier::refund()` merges options into the refund params and exposes no per-request `idempotency_key` option, so the user-branch create is unprotected. This is reachable on a **manual retry**: `cancel()` (`:63`) treats `REFUND_FAILED` as cancellable (`BookingStatus::isCancellable`, enum `:38`), so re-cancelling a `REFUND_FAILED` booking re-enters `transitionToRefundPending` (`REFUND_FAILED → REFUND_PENDING`, `:229`) → `processRefund`. If the **first** user-branch refund actually succeeded at Stripe but the HTTP response timed out (booking left `REFUND_FAILED`), the retry issues a **second Stripe refund** → double refund. PAY-01 closed exactly this hole for the async reconciler; the synchronous user path remains exposed. (The reconciler's new pre-check only guards the job, not this API path.) | High (financial: real double refund on a user-attached booking retried after a timed-out-but-accepted first refund) | Make the synchronous user branch idempotent like the no-user branch and PAY-01: stop using Cashier's keyless `$user->refund()` for booking-cancellation refunds and route the user branch through a stable-key `StripeService` create (e.g. reuse `createBookingRefund` / `bookingRefundIdempotencyKey`, or pass the resolved `$user->stripe()` client into `createReconciliationRefund`). Optionally add the same pre-existing-refund check before the synchronous create, mirroring `ReconcileRefundsJob::findExistingStripeRefundForBooking`. Do not change refund amount/policy semantics. | **Fixed** (`5259931`, follow-up `e744461`) — both user-present and orphaned-user paths route through `StripeService::createBookingRefund()` with the stable booking refund key; timeout-retry regression coverage added. |

## 2026-05-30 Closure notes — SH-06 ledger reconciliation + SH-03 / SH-04

### SH-06 — phantom "Open" findings reconciled

Eight findings were already fixed in the codebase but still showed **Open**, mis-rating risk for anyone (human or agent) reading the backlog. Verified present in HEAD and flipped to **Fixed** with commit + `file:line` evidence (see each row above):

- **F-26** — `confirmBooking()` row lock + locked-row re-check (`f25273c`; `BookingService.php:111-119`).
- **F-27 / F-28** — `restore()` / `restoreBulk()` TOCTOU closed inside the transactional, row-locked `BookingService::restore()` (`902f912`; `BookingService.php:381-420`).
- **F-29** — legacy `AuthController` tokens now carry `expires_at` (`acac8ca`; `AuthController.php:133-137,264-268`).
- **F-31** — wrong `max_refresh_count` threshold removed from `detectAuthMode()` (`78aa03e`, `5e258e7`; `UnifiedAuthController.php:154-195`).
- **F-33** — `finalizeCancellation()` re-acquires the lock and re-reads fresh (`bd3cff9`; `CancellationService.php:373-385`).
- **F-35** — `HttpOnlyTokenController::login(LoginHttpOnlyRequest)` (`5e258e7`; `:6,45`).
- **F-38** — `handleChargeRefunded()` locks the booking row inside the transaction (`abc3959`; `StripeWebhookController.php:248-250`).

### SH-03 / F-74 — synchronous refund ledger (`6d15690`)

`CancellationService::finalizeCancellation` now writes the `stripe_refund_events` row inside its existing `DB::transaction` (`StripeRefundEventRecorder::record()`, source key `cancellation:refund:{refund_id}`), so a successful cancel-with-refund produces the ledger row immediately — no `charge.refunded` webhook required. F-74 flipped to **Fixed** above.

### SH-04 — `handleChargeRefunded` hardening (`6d15690`; relates to F-38)

`handleChargeRefunded` adopted the INSERT-first `stripe_webhook_events` claim used by `handlePaymentIntentSucceeded`: unexpected processing failures now `markFailed` + return **500** (Stripe retries) instead of being swallowed into a 200, and the illegal `CONFIRMED → REFUND_FAILED` transition is explicitly guarded (logged invalid-state, event marked failed, booking left unmutated, ack 200). No new finding ID — this hardens the path F-38 covered. Tests: `StripeWebhookHandlerTest` (4 new charge.refunded cases) + existing `RefundIdempotencyTest`.

## 2026-06-02 Closure notes — SH ledger reconciliation

The following SH-labelled cleanup work is present in HEAD and no longer pending:

| ID | Fixed Ref(s) | Evidence |
| --- | ----------- | -------- |
| SH-01 | `5259931` | Money-final booking date immutability and mutable-booking repricing guards. |
| SH-02 | `5259931`, `e744461` | Unified stable-key booking refund path plus guarded follow-up. Closes F-76. |
| SH-03 | `6d15690` | Synchronous cancellation writes the refund ledger inside finalize transaction. Closes F-74. |
| SH-04 | `6d15690` | `charge.refunded` uses durable INSERT-first claim and retry-safe failure handling. |
| SH-05 | `fcd323f` | Closed `RefundStatus` projection with fail-closed Stripe normalization. Closes F-73. |
| SH-09 | `fcd323f` | Guarded migration aligns `reviews.approved` DB default with the model. Closes F-44. |
| SH-10 | `fcd323f` | Moderator-gated room readiness endpoint replaces the nonexistent room-status route. Closes F-63. |
| SH-11 | `4108e47` | Production configuration assertion command and traffic-admission wiring hardened. |
| SH-12 | `2f52ade` | Remaining PostgreSQL `migrate:fresh` `->change()` races replaced with single-connection statements. |

## 2026-06-04 Findings — CI Workflow Shellcheck Hygiene (surfaced in T-4 actionlint pass)

Surfaced while reviewing **T-4** (frontend coverage-ratchet): running `actionlint` (Docker `rhysd/actionlint`) over `.github/workflows/tests.yml` flagged pre-existing shellcheck issues in `setup`/`nplusone-detection` jobs that are unrelated to T-4. Not introduced by T-4; recorded here and fixed in the same `chore(infra)` change. **ID note:** originally recorded as F-77 on the `chore/ci-shellcheck-quoting` branch; renumbered to **F-79** at merge (2026-06-10) because F-77/F-78 were assigned on `dev` in the interim — same disambiguation precedent as F-06 → F-67.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-79 | `.github/workflows/tests.yml:33,36` (`setup` cache-key steps) + `:433-436` (`nplusone-detection` → "Report Results") | shellcheck **SC2086** — unquoted runner-provided redirect targets: `>> $GITHUB_OUTPUT` in the two `setup` cache-key steps and `>> $GITHUB_STEP_SUMMARY` on four consecutive `echo` lines in the N+1 summary step; plus **SC2129** (style) on the latter for four individual redirects instead of one grouped redirect. No runtime bug — `$GITHUB_OUTPUT`/`$GITHUB_STEP_SUMMARY` are runner-set paths with no spaces/globs — purely static-analysis hygiene: `actionlint` exits 1, which masks future real findings. | Low (CI-lint hygiene; no runtime impact) | Quote both vars (`>> "$GITHUB_OUTPUT"`, `>> "$GITHUB_STEP_SUMMARY"`); collapse the four N+1 echoes into one `{ …; } >> "$GITHUB_STEP_SUMMARY"` group (resolves SC2129 + SC2086 together). | **Fixed** (2026-06-04, `chore(infra)`) — quoted `$GITHUB_OUTPUT` in both `setup` cache-key steps; N+1 "Report Results" now uses a grouped redirect `{ … } >> "$GITHUB_STEP_SUMMARY"`. `actionlint` re-run over `tests.yml` is clean (exit 0). |

## 2026-06-05 Findings — Time-brittle LocationApiTest date literals (D-1 baseline blocker)

Surfaced while establishing the Infection mutation-testing baseline (D-1): Infection's initial run requires a green suite, but `php artisan test` reported 2 failures in `LocationApiTest`. Both used hardcoded June-2026 date literals that have aged into the past relative to the run date (2026-06-05), so the availability endpoint correctly rejected them with 422 "check-in date must be today or later". Pre-existing on `dev` (last touched by `4367d01` "de-flake time-brittle location date assertions", which itself used fixed 2026-06 dates); independent of D-1 production code. Fixed in scope **with approval** to unblock the baseline rather than deferred, since the fix is a pure test-data correction.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-77 | `backend/tests/Feature/LocationApiTest.php` — `it_filters_rooms_by_availability_dates`, `rooms_endpoint_supports_date_availability_filter`, `room_show_endpoint_includes_active_booking_count` | Three tests hardcoded `check_in=2026-06-03` / booking `2026-06-01..2026-06-05`. Once the run date passed those literals the availability query `check_in` became a past date → endpoint returns 422 (correct behavior), so `assertOk()` failed (2 hard failures on 2026-06-05; the third was latent — its booking ended that day and would fail from 2026-06-06). Time-brittle test data, not a product bug; hidden until the calendar caught up. | Low (test reliability; no production impact) | Replace the fixed literals with `now()`-relative dates (e.g. `now()->addDays(10..16)->toDateString()`) preserving each test's intent (booked window overlaps the query window so room1 is excluded / the active booking is in the future). Matches the relative-date pattern already used elsewhere in the same file (`now()->addDay()`). Optional hardening: extend `scripts/assert-date-correctness.sh` to flag hardcoded `YYYY-MM-DD` literals under `tests/`. | **Fixed** (2026-06-05, D-1) — three methods converted to `now()`-relative dates; `php artisan test tests/Feature/LocationApiTest.php` → 19 passed. Done with approval as a baseline-unblock (outside D-1's mutation-config scope but required for a green initial Infection run). |

## 2026-06-10 Findings — Concurrent-booking stress RCA (out-of-scope discoveries)

Surfaced while investigating the CI stress-test failure (0/50 success). RCA itself delivered separately; this row is hygiene found along the way, not part of the failure's causal chain.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-78 | `backend/.env.example:38-39` | Both `CACHE_DRIVER=database` (Laravel ≤10 name, dead on Laravel 12) and `CACHE_STORE=database` are declared. The dead var is misleading: tuning `CACHE_DRIVER` in a deployment changes nothing (`config/cache.php` reads `CACHE_STORE`), and the stress harness inherits `CACHE_STORE=database` from `cp .env.example .env.testing`, putting the booking-route rate limiter on the same PostgreSQL instance under load-test bursts (production guidance in the same file says redis). | Low | Drop `CACHE_DRIVER`, keep `CACHE_STORE`; optionally set `CACHE_STORE=redis` explicitly in the stress job env (tests.yml) so throttle counters stay off the DB under burst. | Open |

## 2026-06-11 Findings — DB P0 Hardening Pass (schema/constraint integrity)

Source: 2026-06-11 DB remediation verification pass (claim-by-claim source verification of the DB remediation plan, corrections C-1–C-4). **DO NOT FIX** beyond the explicitly scoped P0 lane (`feature/db-p0-hardening`: F-80 guard test, F-81 constraints migration, F-82 pepper). Count note: the source pass referred to "the original 8" findings, but its own verification table enumerates **9** items (P0-1…P2-9); all 9 are logged here plus the 3 new findings surfaced by corrections C-2/C-4 → 12 entries, F-80–F-91.

Out-of-scope marker carried from the source pass: cross-column capture-state rules (`amount_capturable`/`amount_received`/`PaymentStatus` interactions) are **[UNPROVEN — needs PaymentStatus review]** and are intentionally NOT constrained by F-81's migration.

| ID  | File:Line | Issue | Severity | Suggested Fix | Status |
| --- | --------- | ----- | -------- | ------------- | ------ |
| F-80 | `backend/database/migrations/2026_02_12_000001_fix_overlapping_bookings_constraint_soft_deletes.php:17` (+ `2025_12_18_000000_optimize_booking_indexes.php`) | **Overlap exclusion constraint can silently disappear or hold the wrong predicate.** The double-booking guarantee (`no_overlapping_bookings`) is created inside `if (driver === 'pgsql')` — on any non-pgsql engine it vanishes with no signal. Two historical predicate versions exist in the migration chain: the 2025_12_18 version **without** `deleted_at IS NULL` (its `up()` is conditional on constraint absence) and the 2026_02_12 fix **with** it, whose `down()` deliberately restores the lax version — so a DB migrated in an odd order can hold the wrong predicate while `contype='x'` happily exists. No test asserted the constraint predicate (correction C-1: assert `pg_get_constraintdef()` text, not mere existence). | Medium | Guard test in `tests/Feature/Database/CheckConstraintTest.php` asserting the live `pg_get_constraintdef()` predicate (daterange half-open form, `('pending','confirmed')` status filter, `deleted_at IS NULL`), `btree_gist` presence, the `chk_bookings_*` definitions, and a raw-DB overlap insert failing with SQLSTATE `23P01`. | **Fixed** (this pass — P0-1 guard test, `feature/db-p0-hardening`) |
| F-81 | `backend/database/migrations/2026_01_11_000001_add_payment_fields_to_bookings.php:23,45` + `2026_03_23_000003:22` + `2026_05_27_000002:36,42` + `2026_05_02_000002:40` + `2026_03_20_000003:55-57` + `2026_04_29_000003:18` | **No DB-level guard on money columns.** `bookings.amount`/`refund_amount`/`deposit_amount`/`amount_capturable`/`amount_received`, `deposit_events.refund_amount`, `service_recovery_cases.refund_amount`/`voucher_amount`/`cost_delta_absorbed`, and `stripe_refund_events.amount_refunded` are declared `unsignedBigInteger`/`integer`, but Laravel's PostgreSQL grammar ignores `unsigned` → plain `BIGINT`/`INTEGER`; negative amounts and over-refunds (`refund_amount > amount`) are insertable at the storage layer. App layer is the only enforcement. | High | Migration adding `>= 0` CHECKs on all money columns above plus relational CHECKs `refund_amount <= amount`, `deposit_amount <= amount` on `bookings`. Cross-column capture-state rules stay out of scope [UNPROVEN — needs PaymentStatus review]. | Open — migration staged on `feature/db-p0-hardening` (P0-2), **unmerged, pending review** |
| F-82 | `backend/app/Services/EmailVerificationCodeService.php:69,126` | **OTP `code_hash` is unsalted/unkeyed SHA-256 of a 6-digit code.** [CONFIRMED] `hash('sha256', $rawCode)` at both issue and verify sites. The keyspace is 10^6 — anyone with DB read access (backup leak, SQLi, replica) can reverse every active code with a trivial precomputed table and take over pending email verifications. | High | Keyed hash with a server-side pepper outside the DB: `hash_hmac('sha256', $code, config('auth.otp_pepper'))`, fail-closed (exception when pepper unset), pepper sourced from `env('OTP_PEPPER')` via config. In-flight codes invalidate on deploy (acceptable: 15-min TTL). | **Fixed** (this pass — P0-3, `feature/db-p0-hardening`) |
| F-83 | `backend/database/migrations/2026_05_02_000002_create_deposit_events_table.php` (+ `admin_audit_logs`, `ai_proposal_events` schemas) | **"Append-only" ledgers are append-only by convention only.** No prevent-mutation trigger exists on `deposit_events`, `admin_audit_logs`, or `ai_proposal_events` (the only DB trigger is `trg_booking_set_location`), and the Laravel runtime role has full DML. Any code path or operator can UPDATE/DELETE ledger rows undetected. | High | Per correction C-2 this needs a decision memo FIRST (FK semantics per ledger), then prevent-mutation triggers with an allowlist for the sanctioned `actor_id → NULL` update (see F-90) — a blanket trigger as originally drafted breaks user deletion and booking cascade paths (see F-89/F-90). | **Fixed** (2026-06-12) — decision memo `docs/DECISION_LEDGER_IMMUTABILITY_FK.md` (D1) + append-only triggers `2026_06_12_000002` with the sanctioned actor→NULL allowlist; merged to dev `618f1c0` |
| F-84 | `backend/database/migrations/2025_05_09_*`/`2025_11_*` index set vs `2025_12_18_000000_optimize_booking_indexes.php:34-42` | **Redundant single-column indexes on `bookings` (corrected inventory per C-3).** The original plan's drop list was stale — `idx_room_dates_overlap`, `idx_room_active_bookings`, `bookings_room_id_check_in_check_out_index` were already dropped by 2025_12_18. Real prefix-redundancy candidates: `bookings_room_id_index`, `bookings_status_index`, `bookings_user_id_index`, `idx_bookings_deleted_at`, plus missed candidates `idx_bookings_location_id` and possibly `idx_bookings_active_overlap`. | Medium | Derive the inventory from the **live catalog** (`pg_indexes` + `pg_stat_user_indexes` over a ≥2-week window incl. month-end), never from docs-side catalogs; then `DROP INDEX CONCURRENTLY` (migration `$withinTransaction = false`) with recreate-as-rollback. | Open — **catalog rebuilt 2026-06-12 (local migration chain; prod snapshot still pending)**. All six candidates live; constraint-backing `bookings_pkey`/`no_overlapping_bookings` (contype `p`/`x` per `pg_constraint`) excluded from pruning. C-3 partially stale: `bookings_room_id_check_in_check_out_index` is **back** — `2026_02_11_000000_reconcile_legacy_index_ordering.php:32` deliberately re-creates what 2025_12_18 dropped (intentional per DB_FACTS §"Legacy index reconciliation"), so it rejoins the candidate list; `idx_room_dates_overlap`/`idx_room_active_bookings` confirmed absent. DROP decisions deferred: prod `pg_stat_user_indexes` unreachable from dev (`PROD_DB_HOST` is a GH secret; local PG holds only test DBs) — needs prod snapshot ≥14 days incl. month-end; prod `pg_indexes` will also settle whether prod ran 2026_02_11. See WORKLOG 2026-06-12. |
| F-85 | `backend/app/Models/StripeRefundEvent.php` / `bookings` money projection (cross-table) | **Cross-table money invariants are unenforceable as CHECKs and unmonitored.** "Σ refunds across `stripe_refund_events` ≤ booking `amount`", deposit lifecycle vs `deposit_events` consistency, etc. cannot be DB CHECK constraints (cross-table) and no reconciliation job watches them. | Medium | Nightly reconciliation view + scheduled artisan command that flags violators (alert, don't block); build on the reconciliation fields added to `stripe_webhook_events` in 2026_05_18. | **Fixed** (2026-06-12, P1-6) — `reconciliation_refund_drift` view (`2026_06_12_000003`, 4 drift facets incl. Σ-refunds-vs-amount and deposit-lifecycle-vs-trail) + read-only `reconciliation:check-drift` (always exit 0, `reconciliation_drift.*` log metrics, dailyAt 04:10). Note: the 2026_05_18 fields turned out to be failure-context metadata, not money — refund sums come from `stripe_refund_events` (the actual ledger). Merged to dev `f5ffa02`. |
| F-86 | `backend/app/Http/Controllers/ProposalConfirmationController.php:47` + `backend/app/AiHarness/Services/AiOrchestrationService.php` | **AI proposal dual-store divergence risk [INFERRED].** `ai_proposals` (durable) mirrors a cache envelope; `decide()` consults the cache. If cache and DB diverge (TTL races, partial writes), the cache can win over the durable row. | Medium | Assert DB-first write ordering (durable row in the same tx as/before cache populate); make `decide()` authoritative on the DB row with cache as read-through only; add a cold-cache confirmation test. | **Fixed** (2026-06-12, P1-7) — gap [CONFIRMED]: `decide()` 404'd on cache miss despite a valid durable row, and the cache envelope was written before the durable row. Fix: cold-cache fallback in `ProposalConfirmationController` (binding/consumed/expiry guarantees reproduced in SQL; decline now marks the durable row), `storeProposal()` writes DB first with cache populate under `DB::afterCommit`. Regression nets `ProposalColdCacheTest` + `ProposalStoreOrderingTest` (anti-tautology verified: legacy ordering fails 2/3). Merged to dev `f5ffa02`. |
| F-87 | `backend/database/migrations/2025_05_09_074429_create_bookings_table.php:30-31` + `deposit_events.actor_email` + `contact_messages` | **PII sprawl with no erasure path.** `guest_name`/`guest_email` on bookings, denormalized `actor_email`/`cancelled_by_email` snapshots on ledgers/audit rows, no field-level encryption; GDPR-style erasure conflicts with append-only design (single app key ⇒ Laravel encrypted cast cannot shred individuals). | High | Per-subject DEK crypto-shredding design (P2 design doc): PII columns store ciphertext, "forget" = destroy the subject's key; ledger rows stay immutable. Phase from highest-risk tables (`contact_messages`, `bookings.guest_email`). Prereq for clean F-83 enforcement. | Open |
| F-88 | `backend/database/migrations/2026_04_28_000001_create_stripe_webhook_events_table.php` + ledger tables | **Unbounded ledger growth; full Stripe JSONB payload retained per webhook event forever.** No retention, archival, or partitioning policy on `stripe_webhook_events`, `admin_audit_logs`, `ai_proposal_events`, `deposit_events`. | Medium | Phased (revised per C-4, see F-91): payload-NULLing retention job first (keep dedupe key + status); partition only the pure append ledgers; `stripe_webhook_events` stays unpartitioned (or dedupe is split to a slim unpartitioned table) to preserve global idempotency. | Open |
| F-89 | `backend/database/migrations/2026_05_02_000002_create_deposit_events_table.php:48-50` | **NEW — deposit_events cascade-erasure hole.** Current behavior: `booking_id … ON DELETE CASCADE` (verified in live catalog: `deposit_events_booking_id_foreign => FOREIGN KEY (booking_id) REFERENCES bookings(id) ON DELETE CASCADE`) means a hard-delete of a booking **silently destroys its deposit ledger rows** — the financial audit trail the table exists to preserve. The "append-only" property is already violated by schema today, independent of any trigger work. Expected: ledger rows survive (or block) booking hard-deletes — likely `RESTRICT`, matching the soft-delete-only booking policy (`service_recovery_cases.booking_id` already uses RESTRICT). | High | Decision memo on ledger FK semantics (couples with F-83/F-90), then a migration changing `deposit_events.booking_id` to `RESTRICT` (or documented sanctioned cascade). | **Fixed** (2026-06-12) — FK swapped to RESTRICT (`2026_06_12_000001`, memo D2); prune-job/force-delete UX follow-ups tracked in memo actions #4/#5; merged to dev `618f1c0` |
| F-90 | `backend/database/migrations/2026_05_02_000002_create_deposit_events_table.php:52-54` (same idiom: `ai_proposal_events` user FK, 2026_04_29) | **NEW — SET NULL-vs-prevent-trigger conflict.** Current behavior: `actor_id … ON DELETE SET NULL` (verified in live catalog) — deleting a user executes an **UPDATE on deposit_events rows** (that is how PG implements SET NULL), which is by design: `actor_email`/`actor_role` snapshots exist precisely to survive user deletion. Conflict: the F-83 remediation as originally drafted (blanket `BEFORE UPDATE` prevent-mutation trigger) would make **user deletion fail** with a restrict_violation. Expected: any append-only trigger must allowlist the single sanctioned mutation "only `actor_id` changed, to NULL" (e.g. `to_jsonb(NEW) - 'actor_id' = to_jsonb(OLD) - 'actor_id'`). | Medium | Fold the allowlist requirement into the F-83 decision memo; test sweep must include user-deletion paths over all three ledgers. | **Fixed** (2026-06-12) — allowlist folded into the F-83 trigger (`to_jsonb` diff + `IS NULL`); user-deletion paths covered in `FkDeletePolicyTest`/`LedgerImmutabilityTriggerTest`; merged to dev `618f1c0` |
| F-91 | `backend/database/migrations/2026_04_28_000001_create_stripe_webhook_events_table.php:16` | **NEW — partition/dedupe conflict in the proposed ledger-partitioning design.** Current behavior: `stripe_webhook_events.stripe_event_id` carries a **global UNIQUE** (verified in live catalog: `stripe_webhook_events_stripe_event_id_unique`) — this is the webhook idempotency surface (INSERT-first claim). Conflict: PostgreSQL requires the partition key inside every unique constraint, so partitioning by `created_at` (F-88's original design) demotes dedupe to per-partition — a Stripe retry landing across a month boundary would process **twice** (double side-effects on payment paths). Expected: dedupe stays global; never partition this table as-is. | Medium | Revised F-88 plan: retention/payload-NULL first; partition only pure append ledgers; keep `stripe_webhook_events` unpartitioned or split dedupe into a slim unpartitioned claim table first. | Open — confirmed against live catalog 2026-06-11 |
